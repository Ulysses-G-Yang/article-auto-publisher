# ArticleOps 当前主线交接

日期：2026-09-10

本文是当前唯一有效的协作接力入口。没有新的明确任务包就停止写入；旧
`codex_handoff*.md`、旧 `RESUMABLE_SESSION.md` 和旧分支说明不再作为任务来源。

## 1. 当前可信基线

| 项目 | 当前值 |
| --- | --- |
| 仓库 | `D:\Backup\Documents\article-auto-publisher` |
| 唯一主工作树 | `D:\Backup\Documents\article-auto-publisher-worktrees\ai-guided-publication` |
| 长期分支 | `main` |
| 文档批次开始前的主线代码 SHA | `055a2406e596cb56352ed25298d1b9fcaf5f41fe` |
| 本批首个文档 focused commit | `80f2640da878659dc27ffdecb5332108c1204e61` |
| 本批浏览器显示修复开始前的 `main` / `origin/main` | `1123d7ebc4bd5996066a102734a14997c00942ca` |
| 本批浏览器显示修复范围 | 仅小红书新开 headed Chrome 使用原生窗口 viewport；其他平台仍固定 1366×900 |
| 私密发布后半程批次开始前 SHA | `5716e6bc88b1fd8f7793b8a7bb590228117af731` |
| 最新任务状态 | 按用户新指令暂停小红书和新增能力；小黑盒已用本次扫码账号真实发布，发布页七图核验通过，见第 11 节 |
| Git 远端 | `git@github.com:Ulysses-G-Yang/article-auto-publisher.git` |
| 固定 Python | `C:\Users\Administrator\miniconda3\envs\article-publisher-py312\python.exe` |
| CoreUI 代码状态 | 最小可复现源码和生产资产已在 `main`；瘦身提交为 `74288d0e`、`055a2406` |

`main` 是业务和开发的唯一长期基线。本批文档提交后的最终 SHA 必须以 Git
和远端 40 位 SHA 核验结果为准，不能在文档中制造自引用的最终 SHA。

## 2. 任务与提交纪律

- 每个完成任务必须更新本文，并与该任务实现放在同一个 focused commit。
- 只暂存任务文件；禁止 `git add -A`，禁止夹带 `uv.lock`、`data`、真实用户内容或运行产物。
- 推送使用 SSH，推送后核对目标远端分支完整 40 位 SHA；禁止 force push。
- 真实登录、保存平台草稿、公开发布、删除和不确定状态重试都需要单独明确授权。
- 本轮公开发布保持默认关闭；最新授权允许启动当前代码的 5000 Web 并执行一次小红书仅自己可见验收，不启动无关服务。
- 本批新增浏览器显示修复与离线回归：Base 默认仍固定 1366×900，仅小红书类级开关使用
  headed persistent Chrome 的 `no_viewport=True`；排版成功后最多滚动一次唯一精确的「下一步」
  到真实视口并回读 rect/中心遮挡提示，不点击、不导航、不保存，也不改变 DRAFT 成功门槛。

## 3. 分支收口状态

- `main` 已从 `809527f65e49f8c82bd394aa2f5925b365cbac56` fast-forward 到
  `055a2406e596cb56352ed25298d1b9fcaf5f41fe`，再由本批首个文档提交推进到
  `80f2640da878659dc27ffdecb5332108c1204e61`；包含 `378bd2f1` 全部主线提交及
  CoreUI 瘦身/版权指向修正，不从 shared/weibo 历史分支整条合并。
- 17 个非 `main` 远端 head 已由注释标签 `archive/2026-09-07/*` 保全并删除；标签和
  精确 peeled SHA 见 `docs/BRANCH_ARCHIVE_20260907.md`，标签不删。
- 13 个本地旧分支 refs 已删除；本地和远端长期分支均只剩 `main`，未使用 force push。
- 10 个旧 worktree 已 `git switch --detach HEAD`，目录、文件、HEAD 和脏状态均保留；不执行
  `git worktree remove --force`，不得搬移、清理或覆盖运行数据。

## 4. 必须原地保全的本机状态

- canonical worktree 的未跟踪 `uv.lock` 原样保留，绝不读取、暂存、覆盖或提交。
- 旧 `weibo-v043-integration` 的脏 `HANDOFF_CURRENT.md` 原样保留，不夹带到主线。
- 两个本地 stash 原样保留，不上传、不恢复、不在未审查秘密前读取内容。
- 各旧 worktree 的 `data`、`uploads`、`images`、Profile、Cookie、Token 和其他忽略运行目录
  原样保留；本轮只记录根层目录名，不读取其内容。

## 5. AI 配置边界

AI 只提供只读“生成平台建议”，默认关闭。生产 Windows 继续复用管理员本机维护的
`data\production_env.ps1`；现有 launcher 在启动时加载并让进程继承，不新增脚本生成、覆写、
迁移或持久化真实配置。本仓库只保留注释示例，不接触真实文件。

运维配置项仅为：`ARTICLEOPS_AI_GUIDANCE_ENABLED`、`DEEPSEEK_BASE_URL`、
`DEEPSEEK_MODEL`、`DEEPSEEK_API_KEY`。长期 Key 只从本机环境注入，页面临时 Key 仅存当前
Python 进程；二者都不写 YAML、Git、日志、数据库或响应。本机配置是明文文件，应由 Windows
ACL 限制管理员和运行账号，不宣称为加密金库。网页永久保存需先实现管理员鉴权，本轮不做。
所有数据路径和账号/Profile 保持不变。

## 6. 已取消、外部和后续目标

- 完整诊断/脱敏包已取消，不再列待办。
- 后续不再制作 ZIP 或新的发行包；已有 release 仅作为历史回滚参考。
- 数据看板属于项目外部系统，本仓库不接管其代码、部署或数据。
- 分支收口后已开始第一阶段：单平台仅自己可见真实发布。小红书用户手动验证及后半程代码状态见第 8 节。
  AI 真实选项写入、多平台多账号仍是后续目标，不是当前已有能力的宣称。

## 7. 验证与最终状态

CoreUI 定向测试已在当前 `main` worktree 通过：`tests/frontend/test_coreui_source_trim.py -q`
为 `4 passed`；该测试文件 Ruff 为 `All checks passed`，示例 PowerShell 仅做 ParseFile 语法
校验且为 `0 errors`。固定 Python 3.12 全量 `pytest -q` 实测为 `1251 passed, 7 skipped`
（69.43s）。首个 focused commit 的 `git diff --check`、SSH 推送和 `origin/main` 完整 40 位
SHA 核验均已通过；删除前后 17 个归档标签 peeled SHA 均匹配，最终本地/远端只剩 `main`。
此前文档批次未启动服务、未登录平台、未保存草稿、未发布、未删除用户数据，也未重试任何不确定副作用；本次真实小红书检查的状态见下节。

### 2026-09-07 小红书浏览器显示修复（本批源码变更）

- 本批从 `1123d7ebc4bd5996066a102734a14997c00942ca` 开始；Base 默认 viewport 行为保持不变，
  仅 `XiaohongshuPlatform` 使用 `no_viewport=True`，让新开的 headed Chrome 页面随实际窗口尺寸变化。
- 小红书排版 snapshot 保留原 `next_visible`/`save_visible` 语义，仅附加真实视口宽高、唯一按钮
  rect、完整落入视口和中心未被覆盖的提示；`prepare_next_step_visibility()` 只在排版成功后的
  `apply_cover` 路径最多调用一次，展示检查失败只附加 UI 提示，不把已完成排版改为失败。
- `human/simulator.py` 在原生视口下从页面实时 `innerWidth`/`innerHeight` 取鼠标范围，并对窄窗口
  做非负边界夹取；动作次数与节奏不变。
- 上一批现场记录：新开浏览器会采用本批修复；当时已运行的旧 helper/窗口未热更新，未重启、未接管，
  不能把源码修复写成旧窗口已生效；当时现场停在排版结果页，未点击「下一步」、未暂存离开、未发布。
- 本批离线验证：固定 py312 `compileall` 通过；定向 `probe/media/layout/viewport` 共 80 passed；
  全量 `pytest -q -p no:cacheprovider` 为 1259 passed、7 skipped、1 个既有 aiosqlite 线程告警。
  `git diff --check` 通过；全仓 Ruff 仍为既有 143 项，新增 XHS 与测试代码无 Ruff 项，Base 与
  `human/simulator.py` 仅报告本批前已存在的基线项。未用当前真实 helper 做 native-resize 烟测；
  未启动或重启生产服务或真实账号浏览器，仅隔离本地 HTML 测试启动过临时无账号 Chrome。

### 2026-09-07 用户授权后浏览器切换（当时只读现场）

- 用户已同意关闭旧窗重开；旧 helper 及其 Chrome 子树已退出、租约已释放，其他 Chrome 未动。新 helper/session
  已使用 `f4098b5c98c7b407e86239826f82fa827ce5ab63` 源码、canonical 账号/Profile；身份昵称匹配通过。本文不记录
  任何昵称、账号 UUID、平台 UID 或路径敏感值。
- 新页面为原生视口：`page.viewport_size=None`，页面 `innerWidth/innerHeight=1520/853`，窗口外框为
  `1382/918`。当前在长文 landing 页；“新创作”和“草稿箱”入口均在屏内。尚未进入编辑器，因此“下一步”不是
  当前页面控件，不能写成已验收下一步现场可见。
- 未点击新创作、未上传、未保存、未发布；云端草稿/仅自己可见能力仍未验证；旧文章是否持久化尚未回读，不能写成
  已恢复。

### 2026-09-07 小红书完整 Word 首次发布前检查（历史步骤）

- 代码基线为 `fcc153654be3c49d3bc2a912fbff3e15ac07b4d8`；指定完整 Word 的离线内容契约已通过：35 字标题、17 个普通正文块、5 个 H2、7 张图片、29 个逻辑块。
- 真实账号复用了 canonical 账号库，没有复制 Cookie；一次身份 ID 与昵称匹配、空编辑器确认、H2 指纹与正文图片指纹匹配均通过。本文不记录任何账号 UUID、昵称或平台 UID。
- 仅执行一次正文填充：`title_readback_ok`、`text_ok=true`、7/7 图片上传、`media_status=completed`。
- 仅执行一次长文排版：`success=true`、`safe_to_continue=true`、7/7 图片加载；图文顺序与 H2 样式稳定回读通过，排版预览接口 2xx 门通过。
- 排版完成后的只读复核阶段 `navigated_to_editor=false`（不代表首次进入编辑器未导航）；页面已处于排版结果态，原编辑器定位不适用，不能把旧定位返回的图片数 0/标题数 0解释为失败。排版后截图抓取失败仅属于截图问题；未改文章状态、未重试。尚未点击「下一步」、尚未「暂存离开」、尚未发布；“仅自己可见”能力仍未确认。
- 云端草稿门禁与全局公开发布开关均未修改；生产 Web/MCP 未启停；临时辅助脚本只在系统临时目录，不进入 Git。
- 当时暂停点为等待用户在排版结果页点击一次「下一步」并提供设置截图；该条不是当前待办。后续用户手动私密发布证据和当前自动化边界见下节，不能用后续结果倒写本次首次检查的动作。

## 8. 2026-09-07 小红书私密发布后半程（最新任务）

### 已确认的现场事实

- 用户在同一既有账号中完成「下一步 → 更多设置 → 仅自己可见 → 发布」，随后在
  `https://creator.xiaohongshu.com/new/note-manager` 提供了新文章卡片截图：卡片自身有「仅自己可见」，
  最初有「审核中」，后续截图不再显示该角标，用户明确回复「成功了」。这是用户手动成功证据，
  不是本批新代码自动执行的验收证据；仅凭角标消失不能由程序宣称审核通过或 `PUBLISHED`。
- 原 Word 的 7 张图片和排版后的 10 页图片是不同计数；不得要求最终页数等于原始图片数。
  最终笔记标题字段的实际值尚未通过 DOM 读取，不能把截图中的可见标题前缀认定为自动截断。

### 本批具体实现及调用边界

- 仅修改 `platforms/xiaohongshu.py`，新增 `tests/test_xiaohongshu_private.py`，同步本文和
  `docs/XIAOHONGSHU_RISK_CONTROL.md`。未改 Word 解析、其他平台、平台目录、前端、数据库或服务入口。
- `prepare_private_visibility()`：绑定当前已确认身份缓存、同一 Page、最终笔记标题、冻结正文及
  发布前实体 ID 基线；验证已观测发布页后最多点击一次「下一步」，等待最终设置出现，选择
  「仅自己可见」并等待控件折叠回读。不会触发发布。
- `publish_private()`：必须显式传入 `confirmed=True`，再次检查绑定的身份、页面、标题、正文和
  私密可见范围；prepare/publish 共用实例锁，发布点击最多一次。点击返回或超时都不表示已发布，
  先返回 `PUBLISH_RESULT_UNKNOWN` 与具体中文原因；后续调用不能重新点击。
- `verify_private_result()`：只读检查同一主文档、同一卡片中的实际实体 ID/属性、精确笔记标题和
  「仅自己可见」，拒绝基线旧 ID、外部传入的假 ID、邻居卡片和 iframe 充当证据。
  新实体已证明时返回「已提交 / 审核中」；没有审核角标时返回「已提交 / 审核状态待确认」，
  不退回重试、不冒充 `PUBLISHED`。未识别状态或无法证明实体时仍保留明确未知结果。
- 这些方法是适配器级入口，尚无网页/MCP/批量编排调用方；`BasePlatform.publish`、`save_draft`、
  `publish_now`、小红书 `delivery_enabled` 和全局公开发布开关均保持原状。
  **拉取本次代码不等于网页已能一键私密发布，小红书本地草稿也没有被改成云端草稿。**
- 管理页卡片、实体 ID 和状态定位器必须由调用方基于真实 DOM 提供，本批没有编造平台选择器或接口。
  测试里的卡片属性、ID、标题均为本地合成数据，不代表已完成真实平台字段采集。

### 验证、真实副作用和下一步

- 固定 py312：本批新增 19 个本地浏览器测试；最后一次审核状态缺失分支修正的定向回归为
  `1 passed, 18 deselected`。修正后的最终全量为 `1278 passed, 7 skipped, 1 warning`（141.31 秒），
  告警仍为既有 aiosqlite 工作线程 `Event loop is closed`，未掩盖或在本批扩大修复。
  两个本批 Python 文件 Ruff 均通过；全仓 Ruff 仍有 143 项既有问题；`git diff --check` 通过。
- 新测试使用无账号的临时 headless Chrome，目标 HTTPS 页面由本地 route 返回合成 HTML，
  其他请求 abort。覆盖延迟 DOM/菜单折叠、身份/标题/URL/正文变更、并发与超时单次点击、
  真实节点 ID 比对、邻居卡片、同页 iframe、状态缺失和缓存回执。
- 本批未新增真实文章、未登录、未上传/保存/发布/删除；未关闭当前受控浏览器及 helper，
  未触碰真实 Profile/Cookie/Token，未启动或重启 Web/MCP。原有文章保持原状。
- Native 只读截图可见目标卡片，但当时可访问性读取被 Windows 当前 URL 校验拒绝；已停止，未换通道绕过。
  后续用户已提供首页文章查看链接，处理状态见第 9 节；不再要求用户从创作服务平台寻找查看链接。
  不要重复发布已成功文章。真实新文章自动化验收及网页/MCP 接入须按下一任务明确授权和验证，
  不能从本地测试推导完成。
- 提交只含上述四个任务文件，推送 `main` 并核对远端 40 位 SHA；最终 SHA 以 Git 为准，不在本文件自引用。

## 9. 2026-09-07 用户补充首页笔记链接

- 本批前 SHA：`f025cab0ce89d18c91ab91c645b2a3286abbd692`。用户说明创作服务平台没有文章查看链接，
  改从小红书首页提供了 `https://www.xiaohongshu.com/explore/<24 位小写十六进制 note_id>` 形式的链接。
  实际文章 ID、完整链接及签名参数不进入源码、测试或本文；用户无需继续寻找创作页不存在的链接入口。
- 新增严格的查看链接 ID 提取；当实际管理页同一卡片的已观测 ID 节点是 `href` 时，先提取 ID 再与
  本次期望 ID 比较。只接受已观测的 HTTPS 域名和 `/explore/<note_id>` 路径，不猜短链、其它域名、
  相对路径或创作页地址；不请求该链接，不保存/返回 query 或 fragment。原有裸 ID/属性回读保持不变。
- 链接仅提供匹配样本，不能替代管理页新实体、账号、标题、私密范围和发布前基线证据。代码不会写死
  当前文章 ID，也不会因为首页链接存在就标记当前自动化成功。参数对取 ID 不必要，不代表访问时一定
  不需要签名/登录态；无参数链接无法访问不能作为发布失败证据。
- 本轮只读恢复了已有创作平台窗口，没有打开新 Profile，没有点击任何文章业务按钮；窗口未返回
  可访问性内容，因此没有新取得管理页实体结构，不绕过安全校验、不切换方式抓取被拦内容。
- 当前缺口是程序从实际投递页面取得同一新实体 ID 的自动采集与真实验证，不再是用户未提供文章链接。
  新代码仍未接入网页/MCP；现有文章不重发，服务、登录态、全局公开发布开关不改。
- 定向链接/同卡片匹配测试为 `16 passed, 19 deselected`；固定 py312 最终全量为
  `1294 passed, 7 skipped, 1 warning`（172.72 秒），告警仍为既有 aiosqlite 工作线程关闭事件循环。
  两个本批 Python 文件 Ruff 通过；全仓 Ruff 仍为既有 143 项，未扩大修复；差异检查通过。

## 10. 2026-09-09 小红书私密发布最小闭环

### 本轮范围与具体改动

- 本批前 SHA：`f42a1acfb1e3c2e5e9a6cbbb585449cf647dea96`。用户要求工具只负责发布，
  取消文章查看链接、实体 ID 和后续审核/数据采集作为交付门禁；第 8、9 节保留为历史记录。
- 网页选择小红书时仅有「仅自己可见」模式，必须手动启用并确认一次；现有草稿全选不自动
  包含小红书。私密模式单独传递为 `PRIVATE_PUBLISH`，只允许小红书；MCP 草稿目标结构不变。
- 后端复用冻结文章、账号租约和实时身份核验，再复用已有文字、H2、原图顺序、一键排版，
  进入下一步、选择仅自己可见并回读，最后只点一次发布。其他平台适配器及草稿门槛不变。
- 收到成功提示或本次同页跳转笔记管理页后返回 `SUBMITTED`，页面映射「已提交（仅自己可见）」。
  只表示平台接收，不表示审核通过；计划同步进入终态。不再要求文章 ID/URL，不进入数据映射补偿。
- 没有明确回执则结果未知，不自动重发；同一执行单重复执行不会再次发布。公开发布开关仍为 false，
  小红书云端草稿 `delivery_enabled` 仍为 false。没有新增路由、数据库迁移、外部服务或 ZIP。

### 验证与当前真实状态

- 定向网页/服务/私密适配器测试：45 passed。完整离线回归：1304 passed、7 skipped；
  1 个既有 aiosqlite 线程关闭告警。包含网页导入七图 Word、目标保存、一次确认、执行结果同步、
  不重复投递和公开发布仍关闭的隔离测试。不能将这些隔离测试当作真实平台验收。
- 初次并行全量遇到 Windows 子进程初始化失败（0xC0000142）；改为单轮全量执行后通过。
  同时补齐新增平台能力字段的既有契约断言；没有为了通过测试修改其他平台业务。
- 本轮变更 Python Ruff、三个 JS 语法检查和 diff 检查通过；全仓 Ruff 仍为既有 143 项。
- 此记录提交时尚未新增真实文章。下一步在 5000 运行本主线，复用 canonical 数据与原账号，
  按用户授权执行指定完整 Word 的一次私密发布；失败/未知不重试，并追加实际结果。
- 启动配置文件目前未找到；不生成 `production_env.ps1`，不创建空账号库替代旧数据。
  本轮仅需 Web；MCP 配置与启动不在范围内。

### 2026-09-09 14:03 用户要求启动服务供手动测试

- 已从当前主工作树绝对入口 `run_flask_production.py` 启动 Web，PID 24988，监听
  `0.0.0.0:5000`；运行代码提交为 `1631e8c4152cb8479c0037d7411d4cdedfa3c2db`。
- 复用 canonical `data`、`account_sessions`、`content_studio` 及原 Profile 路径；
  不复制、清空或替换账号数据。启动前只读确认无 QUEUED/RUNNING 投递，不自动续跑旧任务。
- 本机 `http://127.0.0.1:5000/upload` 与内网 `http://10.0.0.28:5000/upload` 均为 200。
  公开发布、旧上传队列和账号心跳均关闭；8765 MCP 未启动，不影响网页手动测试。
- 使用进程级随机强 APP_SECRET_KEY，未写入磁盘配置、未输出密钥；这不会替换平台登录态。
  未触发真实登录、上传、保存或发布。真实私密发布结果仍待用户本次测试确认。

## 11. 2026-09-10 小黑盒真实公开发布

### 当前授权与结果

- 用户明确暂停小红书、AI 和其他新增能力，只推进现有平台真实发布；随后明确授权使用本次扫码的新小黑盒账号，并要求继续执行。
- 新账号经实时身份验证后完成绑定切换，账号活动表记录了用户授权与旧、新展示名；历史投递快照保留。本文不记录账号 ID、昵称、Profile 路径或登录凭据。
- 本次沿用原 Word 的既有冻结版本：7 张图片、6 个 H2。先保存一次真实草稿，按保存前空列表与新增实体绑定证明保存成功；重开同一草稿后标题与完整图文顺序均通过。
- 发布前发现社区被原选择逻辑误选为无关游戏，停止于发布按钮前。随后仅恢复同一已保存草稿，把社区改为实际搜索得到的“数码硬件”，保留“显示器”话题；没有再次上传正文或新建第二份已保存草稿。
- 正式发布按钮仅点击一次，平台跳转到本篇 `/app/bbs/link/<同一数字 ID>` 文章页。作者、精确标题、社区、话题均匹配；执行单状态为 `PUBLISHED`。随后只读重开文章并滚动触发图片加载，7/7 图片均 complete 且 naturalWidth > 0。
- 实际文章地址和执行单 ID 保留在本机账号库及当前任务结果中，未把用户内容、截图、账号数据或临时辅助脚本加入 Git。

### 修复范围与实际调用边界

- 小黑盒空草稿箱使用 `.creator-draft__list .hb-empty` 与“暂无内容”；新增严格可见空态识别，不再把合法空列表判成无法取得基线。
- 从真实 `.hashtag-list-item__title` / `.topic-list-item__title` 取名称，允许专用名称节点与查询精确匹配；拒绝讨论人数及普通搜索回显，社区候选名称必须匹配查询，避免直接选择不相关推荐游戏。
- Base 在社区/话题未完成、图片不完整、封面失败时阻止正式发布。DeliveryService 在任何警告完成分支之前检查发布结果，避免把未确认发布误写为 `PUBLISHED_WITH_WARNINGS`。
- 小黑盒正式发布改为唯一按钮、标题与稳定草稿核对、单次尝试，以及已观察到的本篇文章路由和精确标题确认；删除“整页含已发布”“任意离开编辑器即成功”与盲点第一个“确定”的旧判断。
- 本次通过现有 ContentVersion、DeliveryService 与小黑盒适配器执行，临时辅助步骤用于发布前暂停及恢复同一草稿修正社区。可以确认这次真实发布成功，尚不能宣称网页无人值守一键发布已经验收。
- 公开发布只在本次 DeliveryService 实例显式开启；没有修改全局默认开关、启动 Web/MCP、扩展其他平台或接入 AI。该文章不再重发。

### 检查与协作状态

- 初轮完整回归为 1323 passed、7 skipped、3 failed：两项失败来自本任务开始前已有的未提交前端变更，已验证相同断言在 HEAD 前端源码通过；另一项是父/子 Python 编码不一致，使用继承的 PYTHONUTF8=1 单独复核通过。
- 原有未提交前端、ZOL 和选择契约协作改动保留，未纳入本批。小黑盒既有的一行 selection_query 透传经审查属于本次发布选项链路，纳入本批；Base 既有 ZOL 选择改动不纳入。
- 新增定向检查覆盖真实空态、专用名称、排除无关社区、发布完整性、未知结果不冒充成功、唯一发布按钮、错标题/错路由以及同实例不重复点击。最终检查结果以本节后续记录及当前任务为准。
- 继承 `PYTHONUTF8=1` 后完整回归为 1325 passed、7 skipped、2 deselected、1 warning（376.95 秒）；排除的两项仅为上述既有未提交前端变更引起的契约失败，aiosqlite 线程退出告警保持记录。最后的发布确认加固另经最终定向回归 112 passed（62.54 秒）验证，没有为了验证再次真实发布。
- 本批测试与 DeliveryService 的 Ruff 均通过；相对 HEAD，Base 和小黑盒没有新增 Ruff 违规（既有分别 13→12、36→34）。差异检查通过。提交仅包含本节修复及交接，推送后核验远端完整 SHA，最终值以 Git 和当前任务结果为准。
- 用户随后明确要求不再使用 Codex 内置浏览器打开这些平台网页；后续平台网页查看交由用户常用浏览器。本次成功文章不再访问、不再发布，本轮剩余操作仅为本地检查、提交及 Git 推送。
