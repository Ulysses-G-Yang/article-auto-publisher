# 百家号封面控件只读探测（2026-08-20）

## 边界

- 账号：已登记百家号账号（平台 ID 仅以 `****5072` 核对）。
- 页面：`https://baijiahao.baidu.com/builder/rc/edit`。
- 操作：只打开空白图文编辑器和唯一“选择封面”弹窗；未输入标题或正文、
  未选择文件、未确认封面、未保存草稿、未公开发布。
- Profile 通过账号级独占租约打开；未清理或复制 Cookie/Profile。

## 真实 DOM 证据

1. 编辑器存在唯一可见“选择封面”入口。
2. 打开后出现单个可见 `cheetah-modal` 封面弹窗。
3. 弹窗页签包含“正文/本地上传”“AI 封图”“免费正版图库”。
4. “正文/本地上传”页签内存在一个隐藏文件控件：
   `type=file`、`accept=image/*`、`multiple=false`。
5. 弹窗明确显示“封面预览（3:2）”和“取消/确定”。
6. 同一编辑器页面另有 `accept=video/*` 控件；因此封面实现必须限定到封面
   弹窗及 image accept 控件，绝不能回退到页面第一个 file input。

## 实现约束与下一验收门

- `ContentVersion.cover_strategy/cover_asset_id` 是唯一封面真值。
- 平台适配器只接收执行期解析出的冻结受控资产路径；不得从当前正文块重新
  猜测第一张图，更不得把显式封面替换为正文图。
- `NONE` 返回 `not_required`；素材缺失或控件不唯一时 fail closed。
- 真正成功必须同时满足：上传后 3:2 预览就绪、确认按钮可用、封面弹窗关闭、
  草稿保存成功，并在按唯一标题重新打开草稿后确认封面仍存在。
- 本文只证明控件结构，不把只读探测冒充真实封面上传验收；当前状态保持
  `RETEST_REQUIRED`，直到完成一次 DRAFT-only 持久化重开验收。
