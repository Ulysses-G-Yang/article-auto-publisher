# 小黑盒 Word 草稿验收证据（2026-08-19）

本文件只记录一次脱敏的 DRAFT-only 真实验收结果，不是平台投递开关，也不代表可以公开发布。

## 输入与执行边界

- 输入 Word 文件 SHA-256：`82FCA555865565400EDF0481AC9753A10FD241EDB4F2DFEAEE5BE1D1C545D531`
- 执行模式：`DRAFT-only`
- 计划短标识：`a3d5e4fe`
- 操作短标识：`bc7750c3`
- 本次仅允许一次小黑盒草稿尝试；未执行 ZOL，未重试，公开发布保持关闭。

## 脱敏结果

- 上传前正文校验通过：22 个文本段落。
- 预期正文图片：7 张。
- 运行日志只观察到 2 次图片数量增加；安全响应没有持久化完整上传/失败计数，因此不能把这两个观察值当作最终媒体计数。
- 终态：`FAILED`。
- 稳定错误：`CONTENT_VALIDATION_ERROR`。
- 失败位置：图片处理后第 10 段落缺失或顺序错误。
- 未取得草稿 URL，未完成草稿箱核验，未取得封面终态。
- 本次不是 `RESULT_UNKNOWN`，没有公开文章，也没有自动重试。

## 第二次内部 DRAFT 复验更正（2026-08-19）

- 第二次内部链路曾返回 `DRAFT_SAVED`，但用户在同一账号的真实草稿箱中未看到新增文章；该结果正式更正为 `DRAFT_VERIFICATION_FALSE_POSITIVE`。
- 本轮账号身份标记为 `IDENTITY_UNCONFIRMED`。新账号只记录脱敏公开信息：显示名 `玩家52852173`、脱敏平台标识 `****2173`；不记录内部账号 ID、Profile、Cookie 或 Token。
- 本轮没有采集保存前后草稿箱计数，也没有取得新增具体草稿卡片、图片持久化或封面持久化证据，因此不能把 `DRAFT_SAVED` 升级为真实草稿成功。
- 小黑盒文字草稿、正文图片、封面和草稿箱核验继续保持 `RETEST_REQUIRED`，不晋级为 `REAL_VERIFIED`；本轮没有公开发布，也没有重试。
- 原先的验证器使用全局页面文本子串并允许无标题乐观成功，可能命中导航、编辑器残留或旧同名内容。本批改为保存前后具体可见草稿卡片的实体差异和标题精确匹配，无法建立可靠基线时在保存前 fail closed。
- 当前仓库没有新的真实草稿卡片 DOM 证据；代码只白名单接受可见 `data-draft-id` 条目或带实体段的 `/creator/editor/draft/<opaque>` 链接，并要求可见列表/空态证据。平台结构不匹配时不点击保存，待下一次只读探测确认后再重验。

## 人工现场观察补充

- 用户在本轮浏览器现场确认：编辑器中只看到 2 张图片插入，且图片插入位置与原始图文顺序不符。
- 该观察与运行日志中两次图片数量增长相互印证，但只是编辑器现场观察；没有草稿保存证据，不能升级为草稿成功或最终持久化媒体计数。

## 根因与修复

失败证据指向两项组合问题：图片处理前把文字全部写完、图片再批量处理，且上传前对正文编辑器执行普通中心点击；在 ProseMirror 重建或插入图片时，中心点击可能把插入点移到正文中段，破坏后续段落。

修复已在提交 `ccb57789924fc02ba1bb2306ee08452114c877c4` 推送；媒体失败进度审计已在提交 `927bc45dfc7336dd92443dc8cac7c0708a7e559f` 推送：

1. 按冻结内容块原始顺序交错写入文字与图片。
2. 通过当前可见正文编辑器的 `focus + Selection/Range.collapse(false)` 将插入点置于末尾，不再用正文中心点击。
3. 每张图片后重新定位编辑器并校验已写文字前缀；首个缺失/乱序立即停止后续图片，并保留媒体状态与错误脱敏语义。
4. 后续失败可从账号活动读取仅含数量和状态的脱敏 `MEDIA_PROGRESS` 记录，不包含路径、Token 或正文。

修复后的真实平台重验收尚未进行；在新的人工授权前不得据此晋级为稳定可用。
