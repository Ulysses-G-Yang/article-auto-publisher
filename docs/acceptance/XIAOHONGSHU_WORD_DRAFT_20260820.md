# 小红书带图 Word 草稿真实验收（2026-08-20）

> **2026-08-21 更正：本文件记录的是同一隔离 Profile 内的本地卡片重开，不能
> 证明云端草稿。** 同账号 Cookie 在无 LocalStorage/IndexedDB 的干净上下文中
> 看不到任何长文草稿，且最终页没有“保存草稿”入口。因此原 `DRAFT_SAVED`
> 结论撤销为 `XHS_CLOUD_DRAFT_UNAVAILABLE`；当前只保留账号管理，不开放投递。

## 最终结论

- 账号：`jayoma`（公开脱敏 ID：`****3540`）
- 输入：用户指定的《多设备共用显示器_川升CS40X_去AI优化发布版.docx》
- 模式：`DRAFT`；所有公开发布开关保持关闭
- 草稿标题：`多设备共用显示器，别只盯KVM：先把输入源、USB-C和桌面空间理清楚`
- 最终计划：`c02a8166-0193-4098-9f46-15feebc5a67b`
- 最终操作：`03159bd5-9955-436e-a919-38b1ccf06480`
- 最终状态（更正后）：正文和图片只在同一 Profile 的本地卡片中重开通过；
  当次 `SUCCESS/DRAFT_SAVED` 属于错误晋级，不是云端草稿实体证据。
- 文章映射：`SUCCEEDED`

## 同一 Profile 本地恢复证据（非云端持久化证据）

- 草稿箱计数：`12 → 12`，证明恢复并更新的是同一条草稿，没有重复创建。
- 草稿箱中标题精确匹配：1 条。
- 保存后重新打开：成功。
- 冻结内容逻辑 token：29 个：17 段普通文字、5 个二级标题、7 张正文图片。
- 7 张图片全部转为平台排版资源，并在重开后满足 `complete=true`、
  `naturalWidth>0`；不再以图片节点数量冒充图片持久化成功。
- 二级标题在原始 TipTap 中以 `h2` 写入；排版后以
  `.text-container.heading-level-2` 表示。平台允许标题因跨页拆成连续节点，
  适配器会重新组合文本并同时验证标题样式和图文顺序。
- 封面：平台没有独立封面文件输入。“封面设置”只有有图/无图、作者、字数时长和
  摘要；生成的是平台模板封面，无法证明选用了冻结正文首图。因此
  `FIRST_BODY_IMAGE` 为 `REAL_FAILED`，完整 Word 一键闭环尚未完成。

## 本轮纠错过程

1. 复查旧验收草稿 `XHS完整Word验收-20260820-001` 时，虽然存在 7 个图片节点，
   但图片预览请求均为 403、`naturalWidth=0`。旧文档中“7 图已持久化”的结论无效，
   本文已更正。
2. 单图探测证明原始编辑器上传后只产生临时 `blob:`；只有执行一次「一键排版」，
   平台才调用长文模板/封面接口并生成可重开的远程图片。探测过程留下过一条
   `XHS排版探测-20260820-114507` 草稿，未删除。
3. 指定 Word 首次完整执行写入 29 个块，但本地排版快照 JavaScript 转义错误导致
   操作失败；平台已经自动留下唯一同名原始草稿，因此后续始终走精确标题恢复，
   禁止创建第二条。
4. 只读复核发现该原始草稿的前 28 个块和 7 张图片全部正确，仅最后一个普通段落
   因平台自动保存时序未持久化。恢复逻辑只在“现有内容严格等于冻结版本前缀、
   缺口仅为尾部普通文本”时补尾段；离开并重新打开同一草稿证明落盘后才允许排版。
5. 平台排版会把 H2 改成 `.heading-level-2`，且会跨页拆分；旧的通用段落校验产生
   假失败。新校验绑定真实样式类、合并连续跨页节点，并继续严格验证图片与文本顺序。

## 安全边界与副作用

- 全程未点击公开发布入口，未开启任何公开发布环境变量。
- 失败操作均在本地校验门停止，不自动新建同名草稿；真正的排版只对唯一同名草稿
  执行一次。
- 没有删除平台草稿，没有清理、复制或输出 Cookie、Token、Profile 内容。
- 本轮平台侧新增过一条单图排版探测草稿；指定 Word 草稿最终仍为唯一一条。
- 验收截图保存在本机临时目录，不进入 Git，也不包含 Cookie 或请求凭据。

## 仍未覆盖

- 精确封面素材选择未实现；在平台提供可验证控件或业务明确接受平台生成封面前，
  `can_run_complete_word_draft("xiaohongshu")` 必须保持 `False`。
- 公开发布仍为 `DISABLED`，未验证且不属于本轮目标。
- 话题选择尚未接入；保存普通长文草稿不需要话题。
