# ArticleOps 当前主线交接

日期：2026-09-11

本文是当前唯一有效的协作接力入口。没有新的明确任务包就停止写入；旧
`codex_handoff*.md`、旧 `RESUMABLE_SESSION.md` 和旧分支说明不再作为任务来源。

## 1. 当前可信基线

| 项目 | 当前值 |
| --- | --- |
| 仓库 | `D:\Backup\Documents\article-auto-publisher` |
| 唯一主工作树 | `D:\Backup\Documents\article-auto-publisher-worktrees\ai-guided-publication` |
| 长期分支 | `main` |
| 文档批次开始前的主线代码 SHA | `055a2406e596cb56352ed25298d1b9fcaf5f41fe` |
| 本批首个文档 focused commit | `80f2640da878659dc27ffdecb5332108c1204e61` |
| 本批浏览器显示修复开始前的 `main` / `origin/main` | `1123d7ebc4bd5996066a102734a14997c00942ca` |
| 本批浏览器显示修复范围 | 仅小红书新开 headed Chrome 使用原生窗口 viewport；其他平台仍固定 1366×900 |
| 私密发布后半程批次开始前 SHA | `5716e6bc88b1fd8f7793b8a7bb590228117af731` |
| 最新任务状态 | 小黑盒、ZOL、知乎、什么值得买已真实发布；百家号、小红书、微博暂停。什么值得买原稿完成唯一一次实际公开提交，个人中心显示已发布，公开页作者、标题及完整 26 块/7 图核验通过，执行单 PUBLISHED，见第 19 节 |
| Git 远端 | `git@github.com:Ulysses-G-Yang/article-auto-publisher.git` |
| 固定 Python | `C:\Users\Administrator\miniconda3\envs\article-publisher-py312\python.exe` |
| CoreUI 代码状态 | 最小可复现源码和生产资产已在 `main`；瘦身提交为 `74288d0e`、`055a2406` |

`main` 是业务和开发的唯一长期基线。本批文档提交后的最终 SHA 必须以 Git
和远端 40 位 SHA 核验结果为准，不能在文档中制造自引用的最终 SHA。

## 2. 任务与提交纪律

- 每个完成任务必须更新本文，并与该任务实现放在同一个 focused commit。
- 只暂存任务文件；禁止 `git add -A`，禁止夹带 `uv.lock`、`data`、真实用户内容或运行产物。
- 推送使用 SSH，推送后核对目标远端分支完整 40 位 SHA；禁止 force push。
- 真实登录、保存平台草稿、公开发布、删除和不确定状态重试都需要单独明确授权。
- 全局公开发布保持默认关闭；最新授权是逐个推进现有平台真实发布，仅在已授权任务的 DeliveryService 实例开启。小红书、AI 和新增能力暂停，不启动无关服务。
- 本批新增浏览器显示修复与离线回归：Base 默认仍固定 1366×900，仅小红书类级开关使用
  headed persistent Chrome 的 `no_viewport=True`；排版成功后最多滚动一次唯一精确的「下一步」
  到真实视口并回读 rect/中心遮挡提示，不点击、不导航、不保存，也不改变 DRAFT 成功门槛。

## 3. 分支收口状态

- `main` 已从 `809527f65e49f8c82bd394aa2f5925b365cbac56` fast-forward 到
  `055a2406e596cb56352ed25298d1b9fcaf5f41fe`，再由本批首个文档提交推进到
  `80f2640da878659dc27ffdecb5332108c1204e61`；包含 `378bd2f1` 全部主线提交及
  CoreUI 瘦身/版权指向修正，不从 shared/weibo 历史分支整条合并。
- 17 个非 `main` 远端 head 已由注释标签 `archive/2026-09-07/*` 保全并删除；标签和
  精确 peeled SHA 见 `docs/BRANCH_ARCHIVE_20260907.md`，标签不删。
- 13 个本地旧分支 refs 已删除；本地和远端长期分支均只剩 `main`，未使用 force push。
- 10 个旧 worktree 已 `git switch --detach HEAD`，目录、文件、HEAD 和脏状态均保留；不执行
  `git worktree remove --force`，不得搬移、清理或覆盖运行数据。

## 4. 必须原地保全的本机状态

- canonical worktree 的未跟踪 `uv.lock` 原样保留，绝不读取、暂存、覆盖或提交。
- 旧 `weibo-v043-integration` 的脏 `HANDOFF_CURRENT.md` 原样保留，不夹带到主线。
- 两个本地 stash 原样保留，不上传、不恢复、不在未审查秘密前读取内容。
- 各旧 worktree 的 `data`、`uploads`、`images`、Profile、Cookie、Token 和其他忽略运行目录
  原样保留；本轮只记录根层目录名，不读取其内容。

## 5. AI 配置边界

AI 只提供只读“生成平台建议”，默认关闭。生产 Windows 继续复用管理员本机维护的
`data\production_env.ps1`；现有 launcher 在启动时加载并让进程继承，不新增脚本生成、覆写、
迁移或持久化真实配置。本仓库只保留注释示例，不接触真实文件。

运维配置项仅为：`ARTICLEOPS_AI_GUIDANCE_ENABLED`、`DEEPSEEK_BASE_URL`、
`DEEPSEEK_MODEL`、`DEEPSEEK_API_KEY`。长期 Key 只从本机环境注入，页面临时 Key 仅存当前
Python 进程；二者都不写 YAML、Git、日志、数据库或响应。本机配置是明文文件，应由 Windows
ACL 限制管理员和运行账号，不宣称为加密金库。网页永久保存需先实现管理员鉴权，本轮不做。
所有数据路径和账号/Profile 保持不变。

## 6. 已取消、外部和后续目标

- 完整诊断/脱敏包已取消，不再列待办。
- 后续不再制作 ZIP 或新的发行包；已有 release 仅作为历史回滚参考。
- 数据看板属于项目外部系统，本仓库不接管其代码、部署或数据。
- 分支收口后已开始第一阶段：单平台仅自己可见真实发布。小红书用户手动验证及后半程代码状态见第 8 节。
  AI 真实选项写入、多平台多账号仍是后续目标，不是当前已有能力的宣称。

## 7. 验证与最终状态

CoreUI 定向测试已在当前 `main` worktree 通过：`tests/frontend/test_coreui_source_trim.py -q`
为 `4 passed`；该测试文件 Ruff 为 `All checks passed`，示例 PowerShell 仅做 ParseFile 语法
校验且为 `0 errors`。固定 Python 3.12 全量 `pytest -q` 实测为 `1251 passed, 7 skipped`
（69.43s）。首个 focused commit 的 `git diff --check`、SSH 推送和 `origin/main` 完整 40 位
SHA 核验均已通过；删除前后 17 个归档标签 peeled SHA 均匹配，最终本地/远端只剩 `main`。
此前文档批次未启动服务、未登录平台、未保存草稿、未发布、未删除用户数据，也未重试任何不确定副作用；本次真实小红书检查的状态见下节。

### 2026-09-07 小红书浏览器显示修复（本批源码变更）

- 本批从 `1123d7ebc4bd5996066a102734a14997c00942ca` 开始；Base 默认 viewport 行为保持不变，
  仅 `XiaohongshuPlatform` 使用 `no_viewport=True`，让新开的 headed Chrome 页面随实际窗口尺寸变化。
- 小红书排版 snapshot 保留原 `next_visible`/`save_visible` 语义，仅附加真实视口宽高、唯一按钮
  rect、完整落入视口和中心未被覆盖的提示；`prepare_next_step_visibility()` 只在排版成功后的
  `apply_cover` 路径最多调用一次，展示检查失败只附加 UI 提示，不把已完成排版改为失败。
- `human/simulator.py` 在原生视口下从页面实时 `innerWidth`/`innerHeight` 取鼠标范围，并对窄窗口
  做非负边界夹取；动作次数与节奏不变。
- 上一批现场记录：新开浏览器会采用本批修复；当时已运行的旧 helper/窗口未热更新，未重启、未接管，
  不能把源码修复写成旧窗口已生效；当时现场停在排版结果页，未点击「下一步」、未暂存离开、未发布。
- 本批离线验证：固定 py312 `compileall` 通过；定向 `probe/media/layout/viewport` 共 80 passed；
  全量 `pytest -q -p no:cacheprovider` 为 1259 passed、7 skipped、1 个既有 aiosqlite 线程告警。
  `git diff --check` 通过；全仓 Ruff 仍为既有 143 项，新增 XHS 与测试代码无 Ruff 项，Base 与
  `human/simulator.py` 仅报告本批前已存在的基线项。未用当前真实 helper 做 native-resize 烟测；
  未启动或重启生产服务或真实账号浏览器，仅隔离本地 HTML 测试启动过临时无账号 Chrome。

### 2026-09-07 用户授权后浏览器切换（当时只读现场）

- 用户已同意关闭旧窗重开；旧 helper 及其 Chrome 子树已退出、租约已释放，其他 Chrome 未动。新 helper/session
  已使用 `f4098b5c98c7b407e86239826f82fa827ce5ab63` 源码、canonical 账号/Profile；身份昵称匹配通过。本文不记录
  任何昵称、账号 UUID、平台 UID 或路径敏感值。
- 新页面为原生视口：`page.viewport_size=None`，页面 `innerWidth/innerHeight=1520/853`，窗口外框为
  `1382/918`。当前在长文 landing 页；“新创作”和“草稿箱”入口均在屏内。尚未进入编辑器，因此“下一步”不是
  当前页面控件，不能写成已验收下一步现场可见。
- 未点击新创作、未上传、未保存、未发布；云端草稿/仅自己可见能力仍未验证；旧文章是否持久化尚未回读，不能写成
  已恢复。

### 2026-09-07 小红书完整 Word 首次发布前检查（历史步骤）

- 代码基线为 `fcc153654be3c49d3bc2a912fbff3e15ac07b4d8`；指定完整 Word 的离线内容契约已通过：35 字标题、17 个普通正文块、5 个 H2、7 张图片、29 个逻辑块。
- 真实账号复用了 canonical 账号库，没有复制 Cookie；一次身份 ID 与昵称匹配、空编辑器确认、H2 指纹与正文图片指纹匹配均通过。本文不记录任何账号 UUID、昵称或平台 UID。
- 仅执行一次正文填充：`title_readback_ok`、`text_ok=true`、7/7 图片上传、`media_status=completed`。
- 仅执行一次长文排版：`success=true`、`safe_to_continue=true`、7/7 图片加载；图文顺序与 H2 样式稳定回读通过，排版预览接口 2xx 门通过。
- 排版完成后的只读复核阶段 `navigated_to_editor=false`（不代表首次进入编辑器未导航）；页面已处于排版结果态，原编辑器定位不适用，不能把旧定位返回的图片数 0/标题数 0解释为失败。排版后截图抓取失败仅属于截图问题；未改文章状态、未重试。尚未点击「下一步」、尚未「暂存离开」、尚未发布；“仅自己可见”能力仍未确认。
- 云端草稿门禁与全局公开发布开关均未修改；生产 Web/MCP 未启停；临时辅助脚本只在系统临时目录，不进入 Git。
- 当时暂停点为等待用户在排版结果页点击一次「下一步」并提供设置截图；该条不是当前待办。后续用户手动私密发布证据和当前自动化边界见下节，不能用后续结果倒写本次首次检查的动作。

## 8. 2026-09-07 小红书私密发布后半程（最新任务）

### 已确认的现场事实

- 用户在同一既有账号中完成「下一步 → 更多设置 → 仅自己可见 → 发布」，随后在
  `https://creator.xiaohongshu.com/new/note-manager` 提供了新文章卡片截图：卡片自身有「仅自己可见」，
  最初有「审核中」，后续截图不再显示该角标，用户明确回复「成功了」。这是用户手动成功证据，
  不是本批新代码自动执行的验收证据；仅凭角标消失不能由程序宣称审核通过或 `PUBLISHED`。
- 原 Word 的 7 张图片和排版后的 10 页图片是不同计数；不得要求最终页数等于原始图片数。
  最终笔记标题字段的实际值尚未通过 DOM 读取，不能把截图中的可见标题前缀认定为自动截断。

### 本批具体实现及调用边界

- 仅修改 `platforms/xiaohongshu.py`，新增 `tests/test_xiaohongshu_private.py`，同步本文和
  `docs/XIAOHONGSHU_RISK_CONTROL.md`。未改 Word 解析、其他平台、平台目录、前端、数据库或服务入口。
- `prepare_private_visibility()`：绑定当前已确认身份缓存、同一 Page、最终笔记标题、冻结正文及
  发布前实体 ID 基线；验证已观测发布页后最多点击一次「下一步」，等待最终设置出现，选择
  「仅自己可见」并等待控件折叠回读。不会触发发布。
- `publish_private()`：必须显式传入 `confirmed=True`，再次检查绑定的身份、页面、标题、正文和
  私密可见范围；prepare/publish 共用实例锁，发布点击最多一次。点击返回或超时都不表示已发布，
  先返回 `PUBLISH_RESULT_UNKNOWN` 与具体中文原因；后续调用不能重新点击。
- `verify_private_result()`：只读检查同一主文档、同一卡片中的实际实体 ID/属性、精确笔记标题和
  「仅自己可见」，拒绝基线旧 ID、外部传入的假 ID、邻居卡片和 iframe 充当证据。
  新实体已证明时返回「已提交 / 审核中」；没有审核角标时返回「已提交 / 审核状态待确认」，
  不退回重试、不冒充 `PUBLISHED`。未识别状态或无法证明实体时仍保留明确未知结果。
- 这些方法是适配器级入口，尚无网页/MCP/批量编排调用方；`BasePlatform.publish`、`save_draft`、
  `publish_now`、小红书 `delivery_enabled` 和全局公开发布开关均保持原状。
  **拉取本次代码不等于网页已能一键私密发布，小红书本地草稿也没有被改成云端草稿。**
- 管理页卡片、实体 ID 和状态定位器必须由调用方基于真实 DOM 提供，本批没有编造平台选择器或接口。
  测试里的卡片属性、ID、标题均为本地合成数据，不代表已完成真实平台字段采集。

### 验证、真实副作用和下一步

- 固定 py312：本批新增 19 个本地浏览器测试；最后一次审核状态缺失分支修正的定向回归为
  `1 passed, 18 deselected`。修正后的最终全量为 `1278 passed, 7 skipped, 1 warning`（141.31 秒），
  告警仍为既有 aiosqlite 工作线程 `Event loop is closed`，未掩盖或在本批扩大修复。
  两个本批 Python 文件 Ruff 均通过；全仓 Ruff 仍有 143 项既有问题；`git diff --check` 通过。
- 新测试使用无账号的临时 headless Chrome，目标 HTTPS 页面由本地 route 返回合成 HTML，
  其他请求 abort。覆盖延迟 DOM/菜单折叠、身份/标题/URL/正文变更、并发与超时单次点击、
  真实节点 ID 比对、邻居卡片、同页 iframe、状态缺失和缓存回执。
- 本批未新增真实文章、未登录、未上传/保存/发布/删除；未关闭当前受控浏览器及 helper，
  未触碰真实 Profile/Cookie/Token，未启动或重启 Web/MCP。原有文章保持原状。
- Native 只读截图可见目标卡片，但当时可访问性读取被 Windows 当前 URL 校验拒绝；已停止，未换通道绕过。
  后续用户已提供首页文章查看链接，处理状态见第 9 节；不再要求用户从创作服务平台寻找查看链接。
  不要重复发布已成功文章。真实新文章自动化验收及网页/MCP 接入须按下一任务明确授权和验证，
  不能从本地测试推导完成。
- 提交只含上述四个任务文件，推送 `main` 并核对远端 40 位 SHA；最终 SHA 以 Git 为准，不在本文件自引用。

## 9. 2026-09-07 用户补充首页笔记链接

- 本批前 SHA：`f025cab0ce89d18c91ab91c645b2a3286abbd692`。用户说明创作服务平台没有文章查看链接，
  改从小红书首页提供了 `https://www.xiaohongshu.com/explore/<24 位小写十六进制 note_id>` 形式的链接。
  实际文章 ID、完整链接及签名参数不进入源码、测试或本文；用户无需继续寻找创作页不存在的链接入口。
- 新增严格的查看链接 ID 提取；当实际管理页同一卡片的已观测 ID 节点是 `href` 时，先提取 ID 再与
  本次期望 ID 比较。只接受已观测的 HTTPS 域名和 `/explore/<note_id>` 路径，不猜短链、其它域名、
  相对路径或创作页地址；不请求该链接，不保存/返回 query 或 fragment。原有裸 ID/属性回读保持不变。
- 链接仅提供匹配样本，不能替代管理页新实体、账号、标题、私密范围和发布前基线证据。代码不会写死
  当前文章 ID，也不会因为首页链接存在就标记当前自动化成功。参数对取 ID 不必要，不代表访问时一定
  不需要签名/登录态；无参数链接无法访问不能作为发布失败证据。
- 本轮只读恢复了已有创作平台窗口，没有打开新 Profile，没有点击任何文章业务按钮；窗口未返回
  可访问性内容，因此没有新取得管理页实体结构，不绕过安全校验、不切换方式抓取被拦内容。
- 当前缺口是程序从实际投递页面取得同一新实体 ID 的自动采集与真实验证，不再是用户未提供文章链接。
  新代码仍未接入网页/MCP；现有文章不重发，服务、登录态、全局公开发布开关不改。
- 定向链接/同卡片匹配测试为 `16 passed, 19 deselected`；固定 py312 最终全量为
  `1294 passed, 7 skipped, 1 warning`（172.72 秒），告警仍为既有 aiosqlite 工作线程关闭事件循环。
  两个本批 Python 文件 Ruff 通过；全仓 Ruff 仍为既有 143 项，未扩大修复；差异检查通过。

## 10. 2026-09-09 小红书私密发布最小闭环

### 本轮范围与具体改动

- 本批前 SHA：`f42a1acfb1e3c2e5e9a6cbbb585449cf647dea96`。用户要求工具只负责发布，
  取消文章查看链接、实体 ID 和后续审核/数据采集作为交付门禁；第 8、9 节保留为历史记录。
- 网页选择小红书时仅有「仅自己可见」模式，必须手动启用并确认一次；现有草稿全选不自动
  包含小红书。私密模式单独传递为 `PRIVATE_PUBLISH`，只允许小红书；MCP 草稿目标结构不变。
- 后端复用冻结文章、账号租约和实时身份核验，再复用已有文字、H2、原图顺序、一键排版，
  进入下一步、选择仅自己可见并回读，最后只点一次发布。其他平台适配器及草稿门槛不变。
- 收到成功提示或本次同页跳转笔记管理页后返回 `SUBMITTED`，页面映射「已提交（仅自己可见）」。
  只表示平台接收，不表示审核通过；计划同步进入终态。不再要求文章 ID/URL，不进入数据映射补偿。
- 没有明确回执则结果未知，不自动重发；同一执行单重复执行不会再次发布。公开发布开关仍为 false，
  小红书云端草稿 `delivery_enabled` 仍为 false。没有新增路由、数据库迁移、外部服务或 ZIP。

### 验证与当前真实状态

- 定向网页/服务/私密适配器测试：45 passed。完整离线回归：1304 passed、7 skipped；
  1 个既有 aiosqlite 线程关闭告警。包含网页导入七图 Word、目标保存、一次确认、执行结果同步、
  不重复投递和公开发布仍关闭的隔离测试。不能将这些隔离测试当作真实平台验收。
- 初次并行全量遇到 Windows 子进程初始化失败（0xC0000142）；改为单轮全量执行后通过。
  同时补齐新增平台能力字段的既有契约断言；没有为了通过测试修改其他平台业务。
- 本轮变更 Python Ruff、三个 JS 语法检查和 diff 检查通过；全仓 Ruff 仍为既有 143 项。
- 此记录提交时尚未新增真实文章。下一步在 5000 运行本主线，复用 canonical 数据与原账号，
  按用户授权执行指定完整 Word 的一次私密发布；失败/未知不重试，并追加实际结果。
- 启动配置文件目前未找到；不生成 `production_env.ps1`，不创建空账号库替代旧数据。
  本轮仅需 Web；MCP 配置与启动不在范围内。

### 2026-09-09 14:03 用户要求启动服务供手动测试

- 已从当前主工作树绝对入口 `run_flask_production.py` 启动 Web，PID 24988，监听
  `0.0.0.0:5000`；运行代码提交为 `1631e8c4152cb8479c0037d7411d4cdedfa3c2db`。
- 复用 canonical `data`、`account_sessions`、`content_studio` 及原 Profile 路径；
  不复制、清空或替换账号数据。启动前只读确认无 QUEUED/RUNNING 投递，不自动续跑旧任务。
- 本机 `http://127.0.0.1:5000/upload` 与内网 `http://10.0.0.28:5000/upload` 均为 200。
  公开发布、旧上传队列和账号心跳均关闭；8765 MCP 未启动，不影响网页手动测试。
- 使用进程级随机强 APP_SECRET_KEY，未写入磁盘配置、未输出密钥；这不会替换平台登录态。
  未触发真实登录、上传、保存或发布。真实私密发布结果仍待用户本次测试确认。

## 11. 2026-09-10 小黑盒真实公开发布

### 当前授权与结果

- 用户明确暂停小红书、AI 和其他新增能力，只推进现有平台真实发布；随后明确授权使用本次扫码的新小黑盒账号，并要求继续执行。
- 新账号经实时身份验证后完成绑定切换，账号活动表记录了用户授权与旧、新展示名；历史投递快照保留。本文不记录账号 ID、昵称、Profile 路径或登录凭据。
- 本次沿用原 Word 的既有冻结版本：7 张图片、6 个 H2。先保存一次真实草稿，按保存前空列表与新增实体绑定证明保存成功；重开同一草稿后标题与完整图文顺序均通过。
- 发布前发现社区被原选择逻辑误选为无关游戏，停止于发布按钮前。随后仅恢复同一已保存草稿，把社区改为实际搜索得到的“数码硬件”，保留“显示器”话题；没有再次上传正文或新建第二份已保存草稿。
- 正式发布按钮仅点击一次，平台跳转到本篇 `/app/bbs/link/<同一数字 ID>` 文章页。作者、精确标题、社区、话题均匹配；执行单状态为 `PUBLISHED`。随后只读重开文章并滚动触发图片加载，7/7 图片均 complete 且 naturalWidth > 0。
- 实际文章地址和执行单 ID 保留在本机账号库及当前任务结果中，未把用户内容、截图、账号数据或临时辅助脚本加入 Git。

### 修复范围与实际调用边界

- 小黑盒空草稿箱使用 `.creator-draft__list .hb-empty` 与“暂无内容”；新增严格可见空态识别，不再把合法空列表判成无法取得基线。
- 从真实 `.hashtag-list-item__title` / `.topic-list-item__title` 取名称，允许专用名称节点与查询精确匹配；拒绝讨论人数及普通搜索回显，社区候选名称必须匹配查询，避免直接选择不相关推荐游戏。
- Base 在社区/话题未完成、图片不完整、封面失败时阻止正式发布。DeliveryService 在任何警告完成分支之前检查发布结果，避免把未确认发布误写为 `PUBLISHED_WITH_WARNINGS`。
- 小黑盒正式发布改为唯一按钮、标题与稳定草稿核对、单次尝试，以及已观察到的本篇文章路由和精确标题确认；删除“整页含已发布”“任意离开编辑器即成功”与盲点第一个“确定”的旧判断。
- 本次通过现有 ContentVersion、DeliveryService 与小黑盒适配器执行，临时辅助步骤用于发布前暂停及恢复同一草稿修正社区。可以确认这次真实发布成功，尚不能宣称网页无人值守一键发布已经验收。
- 公开发布只在本次 DeliveryService 实例显式开启；没有修改全局默认开关、启动 Web/MCP、扩展其他平台或接入 AI。该文章不再重发。

### 检查与协作状态

- 初轮完整回归为 1323 passed、7 skipped、3 failed：两项失败来自本任务开始前已有的未提交前端变更，已验证相同断言在 HEAD 前端源码通过；另一项是父/子 Python 编码不一致，使用继承的 PYTHONUTF8=1 单独复核通过。
- 原有未提交前端、ZOL 和选择契约协作改动保留，未纳入本批。小黑盒既有的一行 selection_query 透传经审查属于本次发布选项链路，纳入本批；Base 既有 ZOL 选择改动不纳入。
- 新增定向检查覆盖真实空态、专用名称、排除无关社区、发布完整性、未知结果不冒充成功、唯一发布按钮、错标题/错路由以及同实例不重复点击。最终检查结果以本节后续记录及当前任务为准。
- 继承 `PYTHONUTF8=1` 后完整回归为 1325 passed、7 skipped、2 deselected、1 warning（376.95 秒）；排除的两项仅为上述既有未提交前端变更引起的契约失败，aiosqlite 线程退出告警保持记录。最后的发布确认加固另经最终定向回归 112 passed（62.54 秒）验证，没有为了验证再次真实发布。
- 本批测试与 DeliveryService 的 Ruff 均通过；相对 HEAD，Base 和小黑盒没有新增 Ruff 违规（既有分别 13→12、36→34）。差异检查通过。提交仅包含本节修复及交接，推送后核验远端完整 SHA，最终值以 Git 和当前任务结果为准。
- 用户随后明确要求不再使用 Codex 内置浏览器打开这些平台网页；后续平台网页查看交由用户常用浏览器。本次成功文章不再访问、不再发布，本轮剩余操作仅为本地检查、提交及 Git 推送。

## 12. 2026-09-10 ZOL 正式提交与导读图处理

### 授权、实际操作与当前阻碍

- 用户在指定下一平台为中关村在线后明确要求继续真实发布；随后表示导读图无法自行处理。本轮由程序处理原 Word 首图的原生封面裁剪，不新增平台、AI 或插件。
- 全程未使用 Codex 内置浏览器；沿用项目原有系统 Chrome 及已绑定 Profile。原账号登录和实时身份匹配通过，没有复制 Cookie 或替换账号。
- 本地 9 月 4 日成功记录中的原草稿与这次小黑盒使用的 Word 冻结版本图文完全相同：26 个内容块、7 张图片。只读重开原草稿，完整图文和顺序核验通过，没有重新创建文章或上传正文。
- 真实页面要求导读图和关联话题。已精确选中“电脑外设”；“显示器”查询的歧义结果没有被盲选。
- 原生导读图裁剪弹窗使用带空格的“确 定”，旧代码未识别。第二次选择同一原图前已观察到第一次弹窗消失、封面仍为空；未重试任何发布或保存。随后程序在明确的裁剪窗口点击一次确认，页面显示上传成功并出现导读图预览。整个区域曾读到三个 img，不能据此认定三个真实封面项。
- 对同一原草稿只点击了一次“存草稿”。返回响应未通过原有成功码检查，执行单记为 `RESULT_UNKNOWN`，稳定错误码为 `DRAFT_RESULT_UNKNOWN`；立即停止写操作。正式发布按钮点击次数为 0。
- 随后的只读核对再次确认账号匹配、原草稿正文和七图完整，但新增导读图与话题均未保留。平台拒绝或未确认保存的具体原因尚未查明，不能归因于用户操作或宣称已修好保存接口。
- 真实辅助会话均已关闭；没有重试该执行单、再次保存或正式发布。后续如需重新设置并保存，须先明确取得这次重试授权，并记录平台安全错误码；不要重复上传正文或创建新文章。

### 本批代码与验证范围

- ZOL 新增正式提交方法：已绑定草稿、当前标题、完整正文、匹配话题与横竖两张已加载导读图均满足后，只点击一次真实 `.foot-item`“发布”控件。
- 当前页面实际加载的发布脚本使用 `/api/v1/creator.content.save.orther` 并在成功后显示“提交成功”。代码仅认可平台已知 API 主机上该路径 POST 的当前草稿、标题、发布类型匹配及明确 `errcode=0` 回执；未知结果不重试。尚未真实触发该提交，因此不能将本批离线测试写成正式发布验收。
- Base 与 DeliveryService 仅为 ZOL 的明确公开投稿回执接入既有 `SUBMITTED` 终态；不编造文章 URL、不启动审核采集或映射补偿。图片/封面未完成、回执来源或公开范围不匹配时不晋级成功；原公开开关保持关闭。
- 四处通用 `SUBMITTED` 文案改为“已提交平台”，防止 ZOL 公开投稿被错误显示为仅自己可见；发布模式仍由原有模式字段表达。
- 导读图改为识别带空格的原生确认按钮，要求本次裁剪生成新的横竖两张封面项，并全部加载。记录有序图像摘要，重开后数量、内容和顺序必须一致；旧图未替换、部分替换、少图、变图、横竖顺序变化均失败。官方已加载前端的裁剪完成回调要求两项；选择器限定 uploader 的真实图片项，排除辅助预览，不能把全区域 img 数量硬写成三张。
- 草稿保存错误仅补充有限数字平台错误码，帮助下次定位；不输出原始响应、图片签名链接或身份参数。用户内容、运行数据库、截图与临时网页脚本不进入 Git。
- 本轮纳入之前保留的 Base ZOL 选择触发及 ZOL 一行 selection_query 透传，它们属于此次发布必填话题链路。原有 AI/前端选择改动与其测试继续保留，不夹带提交；content-studio.js 仅暂存通用状态文案这一行。
- 最终离线检查及推送 SHA 以本节后续记录和当前任务结果为准。当前真实平台结果仍是“原草稿完整，新设置未保存确认，尚未正式提交”。

### 最终检查与交付

- 完整离线回归为 1347 passed、7 skipped、2 deselected（266.65 秒）；排除项仍为第 11 节已证明来自原有未提交前端变更的两项契约失败。保留 1 个既有 aiosqlite 线程关闭告警，不将这次结果表述为全仓无失败、无告警。
- 完整回归后，进一步把封面规则收紧为实际 uploader 的横竖两项，并补齐旧图重排、部分替换反例。最终 ZOL 定向检查 19 passed（75.35 秒），覆盖裁剪确认、重开后完整有序图像、一次提交及明确回执、异常回执不晋级成功；均为本地页面和隔离数据库。
- 新测试与 DeliveryService 的 Ruff 通过；全仓 Ruff 为 140 项既有问题，Base 13→12、ZOL 26→26，按规则与消息比较均无新增。三个 JS 语法检查、差异检查通过；只读审查确认两项封面规则及顺序检查一致。
- 提交范围仅含本批 ZOL、受控提交回执与通用状态文案、定向测试和交接；保留无关协作改动。按根目录约束推送 main 并核验远端完整 SHA，最终值以 Git 和当前任务结果为准。

## 13. 2026-09-10 用户授权 ZOL 再保存一次后完成真实发布

### 实际结果

- 用户明确回复“允许，继续”，授权同一原草稿重新设置导读图和话题并再保存一次；原正式发布授权继续有效。使用原绑定账号及系统 Chrome，没有使用 Codex 内置浏览器、新增插件或替换 Profile。
- 前置一次登录态检查未确认成功，随后只读复核及现有账号服务验证通过；另一次话题搜索无候选，均停在上传/保存/发布之前。最终精确选中“电脑外设”，原 Word 首图经平台原生裁剪上传为竖版 750×1000、横版 1333×1000 两个真实封面项。
- 本次授权下只点击一次“存草稿”。接口 HTTP 200、errcode=0，并返回原草稿 ID。重开后原正文七图、两张封面和话题均保留；没有新建文章或重新上传正文。
- 最初的封面字节摘要核验未通过，但没有再次保存。独立只读重开确认账号、原草稿图文、两张封面的内容和顺序、话题；保存前后封面区域的截图也一致。恢复时重新读取的有序摘要再次匹配，随后直接从该已保存草稿执行正式发布。
- 正式“发布”仅点击一次。同步回执解析返回 PUBLISH_RESULT_UNKNOWN，立即停止写操作；只读作品列表显示本账号全部作品共一条，本篇精确标题、当天 13:44:07、状态“已发布”。作品接口也返回同一实体的明确已发布状态。
- 从作品页本篇“查看 → 查看APP预览”的实际 iframe 取得正式 `/news/<文章 ID>.html` 地址；不是编辑器临时 preview 路径。直接只读打开该页面，19 个文字块按顺序全部匹配，7 个正文图片全部加载。点赞头像等页面图片不计入正文。
- 当前正式发布执行单已依据作品列表与本篇公开页证据，经现有 DeliveryService 完成入口恢复为 PUBLISHED，绑定实际文章 ID/URL，清除该执行单旧错误并追加只读核对日志；此前失败/未知记录保留。没有再次点击发布，也没有启动采集或映射补偿。
- 真实 URL、文章 ID、执行单 ID 和核对证据保留在本地账号库及当前任务结果中；用户内容、截图、原始响应、临时脚本与账号信息均不进入 Git。所有真实浏览器会话已关闭，本篇不再重发。

### 自动化边界

- 这次真实保存与公开发布已经验收；过程中仍需要只读人工核对和本地终态收口，不能宣称网页无人值守一键发布已经完成验收。
- 保存后的封面摘要误判原因尚未精确定位，原始保存前摘要未输出或持久化，不能把“当前能读取两张图”当作此前字节相等的证明。没有放宽数量、顺序或内容检查来伪造成功。
- 从官方页面实际加载的公共壳脚本确认：默认 POST 构造 FormData，Axios 浏览器适配器删除自定义 Content-Type 后由浏览器生成 multipart boundary。原发布请求核对器只解析 JSON/urlencoded，无法提取这类请求的草稿和标题，是明确的误判原因；响应成功码无需放宽。
- 本批只补 multipart/form-data 普通文本字段解析，使用标准 MIME parser；缺 boundary、重复字段或 Content-Disposition、文件、传输编码、无效解码均拒绝。保留原草稿、精确标题、saveType=1、无 isUpdate、明确 errcode=0 和单次点击规则，不以已发布文章重复测试。
- 本批仅追加真实结果交接时，沿用第 12 节已有代码检查结果，执行差异检查；如本批另有解析修复，其定向验证结果在本节后续追加。保留原有无关前端与测试改动，不夹带提交。

### 本批解析修复检查

- 完整离线回归：1357 passed、7 skipped、2 deselected（428.15 秒）；排除项仍是第 11 节已确认由原有未提交前端变更导致的两项契约失败，保留 1 个既有 aiosqlite 线程关闭告警。
- multipart 首轮成功例遇到原测试 200ms 超时过短；正常回执等待改为 2 秒，并断言实际 POST 次数，空回执用例仍保持短超时。原生浏览器 FormData 中文标题、错草稿、重复字段及单次点击均通过。
- 全量启动后追加的传输编码、重复 Content-Disposition 拒绝，与其他 multipart 检查一并通过最终定向 10 项（12.23 秒）；均为隔离页面/数据，没有再次真实投递。
- 新测试 Ruff 通过，ZOL Ruff 相对批次前为 26→26、无新增，全仓仍为既有 140 项；差异检查通过。本批提交仅含 ZOL 解析修复、对应测试与本节真实结果交接，按 SSH 推送并核对远端完整 SHA。

## 14. 2026-09-11 百家号单次提交停在百度安全验证

### 本次授权与真实现场

- 用户授权继续现有平台真实发布，暂停小红书、AI 和新增能力，并禁止使用 Codex 内置浏览器打开平台。此次仅使用项目既有系统 Chrome 和原绑定 Profile；没有新增插件、替换账号、复制登录态或启动 Web/MCP。
- 9 月 10 日和 11 日均通过实时登录及身份核验；沿用 9 月 4 日保存成功的同一原草稿和既有 Word 冻结版本。精确标题、26 个内容块、7 张图片顺序全部匹配，11 日逐图滚动后 7/7 加载成功。
- 当前账号原生编辑响应的 is_preview_gray=0，不需要先执行预览保存。原生页面要求单图封面，并默认勾选“自动生成播客”；该选项 DOM ID 已确认为 ai_tts。
- 前两次准备会话停在播客关闭结果确认：普通 label 点击后即时回读仍为 checked，原生 uncheck 也因即时状态断言失败退出。这两次均为 0 上传、0 保存、0 正式发布；失败执行单保留。官方组件源码为受控状态更新，无取消确认弹窗证据。
- 随后改为对原生 checkbox 点击一次，最多等待 5 秒状态更新，不循环点击。此次确认 checked=false，再将原 Word 首图通过现有原生裁剪流程设置为单图封面；上传一次，封面预览完成。发布前再次核对原账号、同一草稿 ID、完整图文、7 张图片全部加载、唯一已加载封面和无遗留弹窗。
- “发布”按钮仅点击一次，实际 POST 也仅放行一次。请求发出前已验证同一 article_id、精确标题、type=news，以及 activity_list 中唯一 ai_tts 的 is_checked=0。
- 平台返回 HTTP 200、errno=10000015，没有取得成功文章 nid；当前页面显示“百度安全验证”“请完成下方验证后继续操作”，提供滑块和扫码验证。执行单为 RESULT_UNKNOWN / PUBLISH_RESULT_UNKNOWN，未视为接收或已发布，没有再次点击、提交或自动处理验证。
- 本次没有点击“存草稿”，也没有新建文章或重新上传正文。封面和播客关闭仅完成编辑页核验，尚未证明本次设置云端持久化。原 9 月 4 日成功草稿记录保留；不得把本次失败提交写成草稿保存成功。
- 本次真实浏览器已关闭，本地只读复核没有百家号 QUEUED/RUNNING 执行单。安全验证后没有再打开平台网页或尝试提交；真实 ID、用户内容、截图和临时辅助脚本留在本地运行环境，未纳入 Git。

### 实现与后续边界

- Baijiahao.publish_now 新增单次原生正式提交：要求原稿绑定、唯一精确标题、完整正文结构、图片数量与加载、唯一封面及完成发布设置。请求只解析官方已确认的 application/x-www-form-urlencoded；拒绝重复字段、错误草稿/标题/类型、定时、在线修改、付费与其他特殊发布模式。
- 同时核对动态索引活动表单中唯一 ai_tts=0，防止默认播客被带入提交。成功仅认可本次官方 POST /pcui/article/publish?type=news 的 errno=0 和有效 ret.nid，返回 SUBMITTED 与真实文章 ID，不编造公开 URL 或审核通过状态。
- 收口时将实际 helper 已使用的发送前请求守卫加入正式适配器：同一页面最多放行一次匹配请求，保留既有下层守卫，错稿/错模式和前端重复请求在发出前阻止；不是仅在响应后判错。该补充只做离线核验，未再次访问真实平台。
- Base 和 DeliveryService 对百家号明确公开回执增加最小白名单，保留文章 ID，既有 SUBMITTED 终态继续禁止重复执行和文章映射。错误回执即使附带 post_url 也不能回退成 PUBLISHED。
- 根据此次真实安全验证结果补充 errno=10000015 的固定说明，仍归入 PUBLISH_RESULT_UNKNOWN，并保留一次尝试标志，不绕过或自动完成安全验证。
- 最后收口增加官方原生 GET /pcui/article/edit.data.ability.is_preview_gray 监听及草稿绑定；能力缺失或要求预览时在点击前停止。强制预览保存/二次确认流程未接入，不据本账号直接发布能力宣称全部账号可自动发布。该能力收口在安全验证发生后完成，只做离线验证，没有再次真实提交。
- 当前阻碍需要用户本人处理百度安全验证。后续重新提交须取得明确授权，先只读核对原稿与已存在作品状态，再继续同一原稿；不得新建第二篇或通过新执行单自动重试本次未知结果。全局公开发布开关继续关闭。
- 新增测试使用本地页面、无账号临时 Chrome 和隔离数据库，覆盖真实 UEditor 图文结构、受控复选框异步更新、错稿/缺图/缺封面/按钮歧义、动态活动字段、特殊模式、能力门禁、验证码回执、未知结果不重发及端到端 SUBMITTED 文章 ID 保留。
- 本批定向、全量回归、Ruff、差异检查及推送结果在后文补充；既有无关前端与选择测试改动原样保留，不夹带提交。

### 本批最终检查

- 首轮完成定向回归 287 passed（99.05 秒）。完整回归为 1412 passed、7 skipped、2 deselected（273.48 秒）；排除项仍是第 11 节已确认来自既有未提交前端变更的两项契约失败，保留一个既有 aiosqlite 线程关闭告警。
- 完整回归启动后追加适配器发送前请求守卫与前端重复请求反例；最终百家号定向回归 54 passed（52.52 秒），包括错误草稿不发出 POST、重复请求最多放行一次。没有用真实平台重复验证。
- 百家号、DeliveryService、新测试 Ruff 通过；Base 为既有 12 项、regression 测试为既有 7 项，逐规则/消息比较无新增；全仓 Ruff 仍为既有 140 项。差异检查通过，只读复核未发现剩余阻断问题。
- 本批仅提交百家号适配器、最小回执传递/终态保护、相关测试及本文；其他前端、选择契约及未跟踪临时目录保留。按 SSH 推送 main 并核对完整远端 SHA，最终值以 Git 与本次任务结果为准。


## 15. 2026-09-11 微博原草稿进入下一步后要求身份验证

用户最新明确反馈小红书已被风控，要求不再处理小红书。小红书保持暂停，不排查、登录、提交或重试；后续验证排除小红书相关用例，不为该平台另开任务。

### 本次真实结果

- 用户明确让百家号暂停，继续下一个现有平台；本次处理微博头条文章。没有恢复小红书、AI、新插件或其他新增能力，没有使用 Codex 内置浏览器。全程复用项目系统 Chrome 与原绑定 Profile。
- 既有微博账号通过实时身份核验。沿用 9 月 4 日成功保存、与本次其他平台相同 Word 冻结版本的原草稿，26 个内容块、7 张正文图完整且顺序匹配；7/7 真实图片均已加载。编辑器额外的 ProseMirror-separator 图片为光标占位，不计入正文图。
- 原草稿封面为空，“仅粉丝阅读全文”默认勾选。程序仅关闭这一阅读限制，从原生“正文图片”列表精确选择与正文首图 URL 相同的第一个候选，完成一次平台原生 16:9 裁剪上传。封面预览唯一并加载完成，正文与草稿 ID 再核验通过。
- 只点击一次“下一步”。官方当前脚本表明该步骤先执行原生 draft/save、action=2，再打开最终发布配文窗口。本次只放行一个符合原草稿 ID、标题、封面和 follow_to_read=0 的保存请求；自动保存及其他草稿写请求保持拦截。没有创建第二篇草稿，也没有重新上传正文。
- 页面随后跳到“请先验证身份”，响应在跳转后未能读取为成功 JSON，最终发布窗口没有出现。正式“发布”按钮点击 0 次，正式发布 POST 0 次。记录为 RESULT_UNKNOWN / PUBLISH_RESULT_UNKNOWN，不能声称保存成功、平台已接收或真实发布完成。
- 新封面与阅读设置仅完成编辑页验证，尚未证明本次云端保存。原成功草稿记录保留；本次执行单真实 ID、截图、临时脚本与账号信息仅留本机，不进入 Git。
- 没有自动处理验证、刷新验证页或重试保存。真实会话已关闭，本地账号库只读复核微博 QUEUED/RUNNING 执行单为 0。百家号的未知结果也没有被重试。

### 实现范围与下一步

- 微博封面实现替换原有广泛查找/首个按钮点击：限定原生空封面入口、正文图候选及其数量、唯一首图选择、原生裁剪确认、唯一已加载新封面；同实例不重复上传，已有封面不盲目替换。
- 增加原生两步发布：先核对同源编辑地址、草稿绑定、唯一精确标题、完整图文、全部正文图加载、封面及粉丝阅读关闭；“下一步”只允许一次匹配的 action=2 保存，成功回执且最终弹窗出现后才能继续。
- 最终发布限定唯一原生弹窗、含本篇标题的配文与“公开”范围，取消附加关注选项。请求守卫限制当前草稿、精确配文、公开范围、非定时、非粉丝限制、无附加关注/同步/原创声明等字段，最多放行一次正式 POST。验证码、无回执和异常跳转都停止，不能靠旧“发布成功”文字晋级。
- Base 与 DeliveryService 仅增加微博明确原生成功回执的 SUBMITTED 白名单；不编造文章 URL、不触发审核采集或映射补偿。全局公开发布开关仍关闭，只有本次获授权的 DeliveryService 实例开启。
- 最终发布分支目前只通过隔离页面验证，本次真人账号尚未到达该分支；不能把测试或封面成功称为“微博发布已跑通”，也不能宣称网页无人值守发布已经验收。
- 本阶段原待办是由用户本人完成微博身份验证，现已被第 16 节的暂停决定替代。将来若重新保存/提交，必须取得对本次未知结果的明确重试授权，先只读核对原草稿和发布历史，再决定同稿恢复；不要新建第二篇或用新执行单自动重试。

### 验证与交付

- 首轮平台与服务定向回归 218 passed（57.50 秒）。后续补充封面候选、两步请求、终态幂等和错误回执反例，只使用本地页面、无账号临时 Chrome 与隔离数据库；未再次访问真实微博。
- 最终微博定向回归 24 passed（52.59 秒），涵盖真实候选形状的封面首图绑定、裁剪仅一次、原生两步提交、异常回执、附加关注/粉丝范围/定时请求拦截，以及服务 SUBMITTED 终态幂等。
- 微博、新测试与 DeliveryService 的 Ruff 通过；Base 12 项、regression 7 项为既有问题，按规则与消息比较无新增；全仓仍为 140 项既有问题。差异检查通过。
- 全量首次执行发现三个旧微博封面测试只预设四次 evaluate 的返回值，已把这三个用例迁移到原生页面测试，并补上缺入口、空正文图片和空候选场景。迁移后的封面与微博定向回归 48 passed（63.73 秒）；未修改其他平台封面实现。
- 首轮全量为 1433 passed、7 skipped、2 deselected、4 failed（374.02 秒）：三个失败为上述已迁移的微博旧封面测试；另一个为小红书离线私密发布延迟 DOM 用例。用户此时明确要求停止处理小红书，保留失败记录，不排查、不重试该用例。一个既有 aiosqlite 线程退出告警保留。
- 最终回归按用户新范围使用 -k "not xiaohongshu and not xhs"，继续排除第 11 节已确认来自无关未提交前端的两个用例；不会把这次结果称为包含小红书的全仓全部通过。最终为 1301 passed、7 skipped、138 deselected（224.05 秒），无失败；138 项包括小红书相关用例及上述两个既有前端排除项。
- 既有无关前端、发布建议与选择契约改动继续保留，不夹带提交。

- 最后静态检查与暂存差异检查通过；提交仅包含微博适配器、Base/DeliveryService 最小回执白名单、相关测试迁移及本文。通过 SSH 推送 main 并核验远端完整 40 位 SHA，最终值以 Git 和本次任务结果为准。

## 16. 2026-09-11 微博验证繁忙暂停，转入知乎只读核验

### 微博事件记录与暂停决定

- 用户请求本人验证后，使用原账号 Profile 的系统 Chrome 恢复原身份验证入口一次。官方 security.weibo.com/verify/index 页面标题为“出错了”，正文实测为“系统繁忙，请稍后再试（-1）”。验证入口参数仅在本机会话中使用，不进入日志或 Git。
- 随后仅尝试恢复一次原上游 captcha/geetest 入口；该辅助会话未产出可确认的新页面结果，用户持续反馈“系统繁忙”。没有自动解验证码，也没有重试文章保存或发布。不能把上游尝试写成已独立核实相同错误码。
- 用户判断可能与反爬或风控有关，并明确要求先记录、转到下一个平台。归类为“疑似反爬/风控，原因未确认”；现有证据无法区分验证入口过期、平台服务故障或账号风控，不把推测写成确诊。
- 本次辅助会话的文章写请求持续拦截；保存点击、正式发布点击和正式发布 POST 均为 0。第 15 节的一次保存结果仍为 RESULT_UNKNOWN，没有改成成功或创建新执行单重试。
- 用户报告持续繁忙后已关闭对应验证窗口，并按实际命令行与父子关系核实、停止该辅助会话进程；最终未发现该辅助 Python 或原微博 Profile 的 Chrome 残留。本轮记录阶段未重新打开微博。微博现暂停刷新、登录验证、保存和发布；小红书与百家号继续暂停。

### 下一平台：知乎

- 从现有平台中选择知乎，不新增插件、平台或其他能力。只使用项目已有适配器、原绑定 Profile 与系统 Chrome；没有使用 Codex 内置浏览器。
- 本机历史记录显示，本次相同 Word 冻结版本的知乎投递曾在图文顺序检查失败：图片处理后一个二级标题与后续段落合并为一级标题，执行单为 FAILED / CONTENT_VALIDATION_ERROR。较早的成功知乎草稿属于不同内容版本，不能替代本篇验收。
- 本轮实时只读核验通过账号身份比对；草稿箱找到本篇唯一精确同名实体。真实账号信息、文章标题与草稿 ID 仅保留在本机，不进入 Git。找到同名草稿本身不代表正文、图片或发布已验收。
- 随后在写请求拦截下重开该唯一草稿，对照冻结 Word 复核：预期 26 个内容块、7 张正文图，实际 DOM 仅 14 个块、4 张正文图；第三个二级标题与紧随段落合并为一个一级标题，后续内容缺失。只读复核返回 DRAFT_RESULT_UNKNOWN，明确证明现存稿不完整，不晋级为 DRAFT_SAVED 或可发布。
- 本轮浏览器拦截非 GET/HEAD/OPTIONS 请求，包括编辑器自行发起的一次草稿 PATCH；没有输入、上传、点击保存或发布，也不改变历史执行单状态。核验脚本正常退出并关闭浏览器。知乎现有公开发布方法仍为未实现，不能宣称发布已经跑通。
- 当时下一步为修复知乎图文写入并审查同稿恢复方案，现已按第 17 节完成原草稿恢复和完整持久化核验。后续才进入知乎真实发布阶段；不得重建第二篇或重试其他平台未知结果。

### 本批交付范围

- 本批只更新当前交接文档，记录微博暂停与知乎下一平台现场证据；不修改平台实现或配置，不提交临时核验脚本、真实账号数据、截图或用户内容。
- 文档差异核查后只暂存本文，执行 focused commit、SSH 推送与远端完整 40 位 SHA 核验；最终结果以 Git 为准。既有无关前端改动和临时目录保持原状。

## 17. 2026-09-11 知乎分段慢速写入与原稿修复

### 本批修复

- 用户要求先修复知乎，并在现场明确指出输入过快。知乎正文改为每次最多输入 8 个字符、每次间隔 0.16 秒，每个段落完成后另停 0.6 秒；每段文字、每个二级标题和每张图片都必须回读匹配，再继续后续内容，不再整段高速灌入后只在插图时检查。
- 进入下一段前使用真实键盘定位文末，并读取原生 Selection 与 Draft.js block；必须确认新的、空的普通正文块及末尾光标，才允许输入。标题菜单等待原生二级标题选项出现，设置后验证实际标题层级；遇到吞回车、错层级或丢字即停止，不继续制造合并段落。
- 无保存诊断确认，原 Control+A 清空旧稿不可靠，曾留下原正文并追加新文；诊断写请求全部拦截，重开确认原稿未改变。现使用原生文末选择到文首，先验证整个正文与图片均在选区内，再删除，并回读确认正文清空。整篇不支持的块/标题在清空前即拒绝。
- 图片上传绑定打开弹窗前唯一的正文文件控件，避免弹窗新增同形状 input 后定位变得不唯一。上传后等待唯一新增图片取得真实地址并完整加载；任何图片失败立即停止剩余正文，不把图片数量增加当作上传完成。
- 以上改动仅限知乎适配器；冻结 ContentVersion、公共内容语义、其他平台、全局公开发布开关与现有前端改动保持原状。

### 真实修复边界与过程中止记录

- 所有现场操作使用原账号 Profile 的系统 Chrome，每次先实时核对账号及唯一原草稿；未使用 Codex 内置浏览器。沿用第 16 节同一 Word 冻结版本与原知乎草稿，不新建第二篇，不操作微博、小红书或百家号。
- 第一次完整修复尝试在图片控件检查处停止，图片上传请求、草稿保存与公开发布均为 0；随后只读重开确认原稿不变。
- 第二次尝试仅完成前两张图片的加载；第三张图片需要原生上传状态确认 PUT，被本次临时保护规则拦截。过程中共放行 6 个原生图片 POST，保存/发布均为 0；发现旧循环仍继续处理后续图片后，按已核实的进程归属停止该会话，原执行单保留失败，没有冒充完整成功。随后修正临时上传确认规则，并在适配器中增加图片失败立即停止。
- 最终验收使用单独、明确的本次同稿修复执行单。写入期间拦截草稿自动保存与其他文章写请求；只有同一草稿的完整正文、7 图及原生请求内容全部匹配，才允许最多一次原生草稿 PATCH。未知结果不再自动保存或创建另一篇。

### 最终真实结果

- 最终一次写入完成全部 26 个内容块、7 张正文图片，7/7 图片加载完成，所有文字、二级标题、图文顺序与原 Word 冻结版本匹配；本轮冻结封面策略为 NONE，没有上传封面。
- 原生草稿请求仅携带 content、table_of_contents、delta_time、can_reward，没有 title 字段；保存前通过原编辑地址、精确草稿 ID、编辑器标题、完整正文及请求中的有序图片源共同绑定本稿。此前未完整的自动保存全部拦截，最终只放行 1 个完整原生 PATCH，公开发布请求 0 次。
- 保存成功 JSON 没有读到，因此当时先记录 RESULT_UNKNOWN / DRAFT_RESULT_UNKNOWN，停止写入并关闭会话；没有再次保存。随后使用同一账号 Profile 单独只读核验：草稿箱仍为同一唯一标题匹配实体，重开后完整 26 块/7 图与冻结版本精确一致。该只读会话连编辑器自行发起的草稿 PATCH 也已拦截，正常退出并关闭浏览器。
- 依据本次保存前已绑定的原稿 ID、仅一次发送的完整保存请求，以及随后独立只读全文实证，仅将本次修复执行单调和为 DRAFT_SAVED，清理其旧未知错误并保存审计说明。没有伪造 HTTP/JSON 成功回执，没有覆盖原历史失败执行单，没有再次执行平台动作或新增通用调和能力；真实账号、执行单 ID、草稿 URL 与临时脚本仍只留本机。
- 知乎本轮完成的是正文/图片修复与真实原稿保存。公开发布仍未执行，现有 publish_now 仍明确拒绝；后续工作从这份已修复原草稿进入原生发布设置、核对发布条件，不再重写或重传正文。微博、百家号、小红书保持暂停。

### 验证与交付

- 既有知乎基线 54 passed（1.47 秒）。新增隔离 Chrome 编辑器场景覆盖延迟换段、延迟标题菜单、编辑器重建、多行文本、图片前后标题、同形文件控件、旧稿完整清空、吞回车/丢字/标题丢失即停止；不使用真实账号或真实外网完成这些测试。
- 增加冻结 ContentVersion → DeliveryService → 真实知乎 fill_content 的链路验证，确保活动稿修改不影响旧冻结版本，原始实体转义与零宽字符仍保留在内容数据库。最终定向回归 61 passed（38.55 秒），相关文件 Ruff 通过。
- 第一轮扩大回归 1305 passed、7 skipped、138 deselected（275.05 秒），保留一个既有 aiosqlite 线程退出告警。范围继续排除用户暂停的小红书，以及第 11 节已确认由无关未提交前端变更造成的两项用例；不宣称包含这些排除项的全仓全部通过。
- 最终完整范围回归为 1306 passed、7 skipped、138 deselected（268.22 秒），无失败，仍有一个既有 aiosqlite 线程退出告警。最终相关源码与测试 Ruff 通过；全仓 Ruff 仍为 140 项既有问题，没有新增本任务文件问题；差异检查通过。
- 只提交知乎适配器、知乎测试、隔离原生编辑器 fixture、冻结内容投递链路的知乎覆盖和本文，共 6 个任务文件。既有前端/发布建议/选择契约改动及未跟踪临时目录原样保留；临时现场脚本、真实账号与用户内容不进入 Git。按 SSH 推送 main 并核对远端完整 40 位 SHA，最终值以 Git 与任务结果为准。

## 18. 2026-09-11 知乎原稿公开发布验收

### 授权与最终真实结果

- 用户明确授权“允许你公开发布，你发一个试试”。本次仅使用第 17 节已恢复的原知乎草稿、原冻结 Word 版本和原绑定账号 Profile，在系统 Chrome 中执行；没有使用内置浏览器，没有新建文章、重写正文、重传图片或上传封面。
- 最终发布前实时核对账号身份，原生草稿 GET 明确返回 draft 状态；标题、作者 ID、完整 26 个内容块和 7 张已加载图片全部匹配。设置为普通免费文章：未投稿至问题、无声明、关闭送礼物，无定时、付费、专栏或附带回答。
- 最终执行只放行 1 个原生 POST `/api/v4/content/publish`，HTTP 200。原稿未修改，原生 forceUpdateDraft 未产生实际保存；本次成功执行保存请求 0，发布请求 1，其他自动保存被拦截。
- JSON 回执因页面跳转未能读取，没有把 HTTP 200 单独视为成功。随后重新 GET 打开同一文章的公开页面，确认正常响应、原文章地址、精确标题、绑定作者链接、完整 26 块正文及有序 7 个图片标识匹配，另做整页截图人工检查。执行单最终 PUBLISHED、确认令牌已使用、文章 ID 与公开 URL 已入库、错误码为空。
- 真实账号、标题、文章与执行单 ID、公开 URL、图片签名、截图和临时辅助脚本仅留本机，不进入 Git。辅助浏览器已正常关闭；全局公开发布仍默认关闭，只在本任务 DeliveryService 实例开启。其余暂停平台保持原状。

### 最小实现与现场差异

- 知乎发布方法接入原生发布按钮；必须先通过已保存草稿证据进入只读准备，再核验原稿为 draft、作者和冻结正文。已发表文章拒绝更新；一次调用后不允许重复点击。普通 Base 发布链路可用已有完整草稿证据进入相同准备路径，本次真实执行则直接恢复原稿，避免再写入。
- 写请求保护只放行绑定同一原稿、同一正文和图序的最多一次原生 PATCH，以及最多一次普通文章公开 POST。原稿无改动时允许基于完整云端回读直接提交，不强迫制造额外保存。付费、定时、问题回答、错误 ID/标题、缺图、换图或正文缺失均拦截；存在已发出副作用而结果未确认时保留 RESULT_UNKNOWN，不重发。
- 现场首次准备因非 JSON 遥测触发 Playwright JSON 解析异常而停止，保存/发布均 0。修正为仅解析相关文章请求，捕获解析错误且不把原始请求写入异常；加入非 JSON 遥测回归。
- 后续准备分别发现空的话题 combobox，以及 CDN 缩略图/分片和私有图片临时签名差异，均在点击前停止，保存/发布均 0。空话题控件不再被误判成额外发布设置；图片只在已验证知乎媒体域名内按 v2 媒体摘要与顺序比对，不比较会变化的尺寸、分片和签名，不放宽为只看图片数量。
- 第一次原生按钮点击发现 forceUpdateDraft 对未修改原稿不发 PATCH，发布 POST 被本地“必须先保存”的错误条件拦截；该次确认保存/发布请求均未发送，原执行单仍保留当时的 RESULT_UNKNOWN，没有改写历史。根据明确的零发送证据修正本地条件后，才完成上面的唯一一次实际公开请求；不存在对未知已发送发布的重试。

### 验证与交付

- 新增隔离 Chrome 原生发布 fixture，覆盖保存后提交、未修改原稿直接提交、普通发布链路复用草稿证据、错误标题/ID/作者、已发表稿、缺文换图、付费/定时/回答/送礼物、重复请求、HTTP 失败和公开页错稿；所有 fixture 网络由本地路由响应，测试不连接真实账号。
- 图片回归覆盖同一媒体在 1440/720 尺寸、不同 CDN 分片、私有签名地址之间的稳定绑定；不同媒体摘要和非知乎媒体主机仍拒绝。另包含多行文本和空话题控件。
- 最终知乎定向回归 75 passed（94.74 秒）；扩大回归第一轮 1320 passed、7 skipped、138 deselected（355.22 秒），最终完整范围为 1323 passed、7 skipped、138 deselected（319.16 秒），无失败，仍保留一个既有 aiosqlite 线程退出告警。范围继续排除小红书及第 11 节已确认与未提交前端改动有关的两项测试，不声称包含排除项的全仓全部通过。
- 最终任务源码与测试 Ruff 通过，差异检查通过；全仓 Ruff 仍为 140 项既有问题，没有新增本任务文件问题。
- 仅提交知乎适配器、知乎既有测试、新增发布测试和本文。既有无关前端改动与临时目录原样保留；按 focused commit、SSH push 和远端完整 40 位 SHA 核验交付。

## 19. 2026-09-11 什么值得买原稿真实发布

### 真实结果与本轮边界

- 用户明确要求什么值得买也尝试真实发布。本轮使用既有账号 Profile 和项目系统 Chrome，没有使用 Codex 内置浏览器；未新增插件、平台或 AI 能力。全局公开发布开关仍默认关闭，仅本次 DeliveryService 实例开启。
- 当前账号下找到了本篇唯一精确同名原草稿。只读重开确认冻结 Word 的全部 26 个内容块、7 张正文图及顺序完整，7/7 图片加载完成，不重写正文、不新建第二篇。同账号另一篇已发布内容保持原状。
- 平台要求长图与方图封面，原稿两项均为空。通过原生已上传图片列表选择正文首图，只发送一次绑定原稿的原生裁剪请求；HTTP 200、error_code=0，平台确认上传成功。该接口已将两个封面写入原稿，后续只读重开确认长图与方图均存在且加载完成，没有再次裁剪或上传。
- 本篇最终只发送一次原生公开 POST，路径为 `/api/editor/article/submit`，submit_type=submit。绑定账号、原稿 ID、标题、完整正文、有序正文图片、两个封面与原稿创作声明全部匹配。HTTP 200、error_code=0，页面显示“提交成功”和“发布成功”。本轮没有草稿保存请求，也没有重试已发送的发布。
- 后续只读个人中心确认同一原稿已变为“已发布”，正式链接仍绑定同一文章 ID。独立打开公开文章页，核验精确标题、绑定作者个人页链接、全部 26 块正文及有序 7 张正文图片；正式 URL、文章 ID 与证明已保存在本机执行单，最终状态为 PUBLISHED，确认令牌已使用，错误码为空。真实账号、标题、文章 ID、URL、截图、原始页面及辅助脚本不进入 Git。

### 最小实现与现场中止记录

- 什么值得买新增独立的原稿发布模块，适配器复用其原生发布流程。必须先验证账号、唯一原草稿、冻结图文与云端正文；没有完整原稿证据或已发表同名稿时停止。已存在的封面从原稿读取回执绑定，避免再次裁剪。
- 按现版页面实际格式解析 qs 表单编码，涵盖原稿空正文 POST、图片库读取、图片原图读取、图片列表索引与原生发布字段；保留正文图片源和顺序检查。未选择专栏时允许原生请求省略排序字段，其他发布设置仍严格匹配。封面 CDN 地址规范化保留具体长图/方图变体，不把两种封面混为同一 URL。
- 请求保护只放行已绑定的读取、最多一次原生封面裁剪及最多一次原生公开提交；在异步正文核验前占用提交机会，防止并发重复请求穿透。自动保存、手动预览保存、失败后的自动保存以及新建文章请求均拦截。请求已发送而结果不能确认时停止，不自动重发。
- 前两次现场准备分别遇到空正文读取 POST 被误拦、图片库表单编码及控件稳定等待问题，均在裁剪、保存、发布请求为 0 时停止。首次裁剪收到成功回执，但返回封面地址未通过当时校验，执行单保留当时未知状态；通过只读重开证明两个封面已经持久化后，后续直接使用既有封面。之后一次发布点击因缺省专栏排序字段被本地拦截，保存与公开请求仍为 0；修正后才发出上面的唯一一次实际公开请求。
- 正式提交已成功后，临时辅助脚本打开个人中心超时，错误地把导航失败作为执行失败。本轮依据已捕获的唯一提交成功回执、实时个人中心已发布实体及独立公开页全文证明，纠正同一个正式执行单为 PUBLISHED；保留原失败活动日志并追加只读纠正原因，没有创建另一个发布执行单或重发文章。其他准备阶段执行单保持原历史状态。
- Base 与 DeliveryService 接受绑定文章 ID 的什么值得买 SUBMITTED 回执；该回执只表示平台接收，不冒充公开上线。真实任务的 PUBLISHED 来自上述后续公开页核验。复用既有内容和草稿通道，不改写旧草稿保存接口或公共 Word 内容语义。

### 验证与交付

- 隔离 Chrome 回归覆盖原生表单读取与提交、原生裁剪、已保存封面、普通发布链路复用完整草稿证据、错稿/缺文/换图/图片列表冲突、错误封面与声明、未知字段、已发表稿、布尔错误码、裁剪失败、封面未加载以及串行/并发重复请求。所有测试网络由本地 fixture 响应，不连接真实账号。
- 执行单回归验证 SUBMITTED 保存文章 ID、没有伪造公开 URL，再次执行同一执行单不会重新发布。更新旧“发布未实现”断言为“缺少完整原稿证据即停止”，保留发布前阻断要求。
- 定向回归 187 passed（199.32 秒）；补充已有封面发布链路与图片列表冲突用例 2 passed，最后包含并发重复请求的相关回归 6 passed（26.03 秒）。旧占位断言修正后的什么值得买既有回归 10 passed（3.81 秒）。
- 扩大回归 1348 passed、7 skipped、138 deselected（500.78 秒），唯一失败为仍期待 PlatformNotImplementedError 的旧占位断言；该断言随后按上条更新并单独复测通过。扩大运行继续排除小红书与第 11 节既有无关前端改动涉及的两项用例，没有声称修正后重新跑过完整范围或包含排除项的全仓全部通过。
- 全仓 Ruff 仍保留 140 项既有问题，什么值得买适配器、新增发布模块与新增测试无 Ruff 问题，差异检查通过。既有无关前端修改与临时目录保留，仅暂存本任务文件，执行 focused commit、SSH push 与远端完整 40 位 SHA 核验。

## 20. 2026-09-15 登录检查与操作节奏修复

### 本轮范围和实际副作用

- 用户在讨论登录误判、浏览器路线和写入过快后明确要求“那你直接改”。本批完成代码及离线验证，不接入新平台、插件、AI 或采集能力，不把离线测试当成真实平台验收。
- 未登录或刷新真实平台，未新建、保存、上传或发布文章，未修改真实 Profile、Cookie、账号数据库和生产配置；未重启生产服务。小红书、微博、百家号继续暂停，本批只修改其共用动作入口并运行隔离 fixture，不恢复实际账号操作。
- 原有真实发布结果保持第 18、19 节及更早记录；没有重发已成功文章。全局公开发布开关继续默认关闭。

### 已完成的行为修复

- 新增统一登录失败解释器。check_login=false 且没有明确失效证据时报告 SESSION_CHECK_FAILED 或原平台稳定错误码；明确登录页面或未认证回执才报告 LOGIN_REQUIRED，验证码和限流分别保留 CHALLENGE、RATE_LIMITED。未知检查失败不再自动要求扫码，即使允许交互登录也不会因此启动 login()。错误详情不携带原始认证数据。
- 手动账号验证失败显示当前 ERROR，明确失效才显示 LOGIN_REQUIRED；普通检查失败保留最后成功验证时间作为历史，不把它当作当前可用证明。投递前身份失败仍阻止所有发布；稳定账号 ID 比对保持原要求。
- DeliveryService 通过同一 Profile 租约内的稳定身份核验后，仅允许紧接着的 Base.publish 消费一次检查结果，避免再次导航查登录。复用绑定 Context、Page、URL、Profile、asyncio task 和 30 秒有效期；换页面、换任务、关闭或重新初始化即失效，不跨执行单复用。
- 通用启动器保留系统 Chrome 和原独立 Profile，删除 navigator 属性伪装与关闭站点隔离参数，保留原网络出口参数。明确目录占用仍停止，无占用证据的启动失败改为 BROWSER_START_FAILED。头条继续使用既有原生 Chrome + CDP，不统一迁移全部平台，不宣称 CDP 能消除风控。任务结束仍正常关闭本次浏览器，持久资料目录不等于浏览器常驻。
- 现有原生点击、键盘、选择、上传、导航/刷新和已知脚本写动作经过 ActionPacer。正文默认每秒 6 字，动作完成到下一动作至少 1 秒，段落至少 1.2 秒；参数有明确上下界。控制器串行调用，不自动重试部分输入或未知结果。标题/搜索框保留完整原生 fill，富文本标题保留一次完整 insert_text，先预留字数时间；ZOL 特殊结构块保留原子插入。响应和文件选择器的等待预算计入窗口内的动作停顿。
- 停用 ZOL 跨域认证 Cookie 复制，旧兼容钩子不读取 Cookie、只返回 0，旧 allow_cookie_bridge=true 也不恢复复制。使用现有创作者中心原生登录与后端身份核验，不清理已有认证数据。

### 验证和交付

- 新增虚拟时钟测试覆盖字速、串行间隔、部分输入失败不重输、标题一次完整输入及配置边界；隔离 Chrome 覆盖多行 Unicode 正文、认证 UI、真实文件选择器在停顿后仍能收到事件。账号服务测试覆盖错误分类、无关登录阻断、一次身份复用以及跨上下文失效。所有测试使用临时目录和本地路由，不连接真实账号。
- 9 月 14 日扩大回归为 1520 passed、7 skipped、2 deselected（535.87 秒）。两项排除仍是第 11 节已确认受无关前端未提交变更影响的契约测试。本批平台共享逻辑涉及小红书，因此离线回归包含其隔离用例，不代表恢复真实操作。
- 最后补充响应超时预算后的扩大回归为 1518 passed、3 failed、7 skipped、2 deselected（791.86 秒），保留一个既有 aiosqlite 线程退出告警。3 个失败均来自微博 fixture 中固定 20000 毫秒的旧断言，已更新为原等待预算加默认动作间隔，保留新草稿 ID、响应绑定和禁止重复创建的全部检查。包含该修正与最终原子标题补丁的定向回归为 236 passed（34.95 秒）；最终修改后未再次重跑完整套件，不宣称包含排除项或最终全部修改的全仓全部通过。
- 全仓 Ruff 剩余 130 项既有问题，比本批开始时的 140 项减少；逐条对比 HEAD 确认没有新增问题。新增核心模块、账号服务及新增测试 Ruff 通过，git diff --check 通过。既有前端、发布建议、选择契约改动及未跟踪临时目录保留，不进入本批提交。
- 实现说明见 docs/SESSION_STABILITY.md。本批按 focused commit、SSH push 和远端 main 完整 40 位 SHA 校验交付，最终 SHA 以 Git 与任务结果为准；生产服务尚未重启，新的启动配置与降速流程仍需下一次单平台真实任务验收。
