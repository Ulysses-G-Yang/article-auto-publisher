# 微博 Word 草稿真实验收记录（2026-08-21）

## 验收边界

- 平台：微博头条文章。
- 账号：`天上的羊咩咩`，真实 Profile 重新验证为 `VALID`。
- 内容：`多设备共用显示器_川升CS40X_去AI优化发布版.docx`。
- 模式：仅 `DRAFT`；公开发布开关保持关闭。
- 封面：`NONE`，本轮不要求封面。
- 副作用纪律：创建/保存只允许一次；出现平台部分持久化后停止，不自动重试。

## 实际结果

Content Studio 成功冻结了 29 个有序块和 7 张图片。微博创建接口返回成功并生成
草稿 `4183864`，但正文图片链路在原始 `img` 节点计数达到 2 后失败。随后只读打开
草稿箱，得到以下平台侧证据：

- 标题精确匹配的草稿数量：1。
- 标题与本次冻结标题一致。
- 正文可读长度：210。
- 正文原始 `img` 节点：2 个；后续真实 DOM 复核确认它们均为不可见的
  `ProseMirror-separator`，真实正文图片实际为 0 张。
- H2：0 个。
- 没有点击公开发布。

微博会在显式点击“保存草稿”之前自动持久化编辑器内容，因此本次不是可安全重试的
普通失败。账号执行单和计划目标已校正为 `RESULT_UNKNOWN`，并保留“禁止自动重试”
审计记录；生产平台目录继续保持 `delivery_enabled=False`。

## 本轮修复

1. 草稿列表 JavaScript 换行正则改为原始字符串，避免浏览器解析失败。
2. “写文章”改为唯一可见 `button`，不再误点同名外层 `div`。
3. 新草稿必须同时满足：创建接口 2xx、hash 切换到不同的正整数草稿 ID、编辑器
   初始状态可证明；任一证据不完整即停止。
4. 新草稿创建后的标题/正文/图片异常统一记为 `DRAFT_RESULT_UNKNOWN`。
5. 计划状态机处理 `RESULT_UNKNOWN`，不再永久停留在 `EXECUTING`。
6. 只读探测工具可按精确标题打开唯一草稿，并只返回字数、图片数、H2 数和 DOM
   形状，不输出正文、Cookie、Token 或本机图片路径。
7. 用户已明确选择修复现有草稿，不删除、不新建第二篇；验收入口只允许标题精确
   唯一且 draft ID 为 `4183864` 时进入覆盖流程，生产工厂不接受该恢复开关。

## 图片失败根因证据

在所有非 GET 请求均被拦截的条件下，重新打开草稿 `4183864` 的正文图片弹窗，
确认：弹窗 1 个、正文文件控件 1 个、`accept` 为微博当前图片格式白名单、没有残留
图片选中项，“插入”按钮在未选择图片时禁用。该探测没有选文件、没有保存。

随后核对微博当前编辑器的官方动态模块：

- 本地文件上传成功后只会在图片库新增 `.image-item`。
- 必须点击该条目，使其出现 `.is-selected`，之后“插入”按钮才会启用。
- 官方上传请求的超时窗口为 120 秒；原适配器只等待 15 秒，并且没有点击新条目。
- 本次 Word 第三张 PNG 为 1,859,013 字节，明显大于前两张，旧等待窗口使它更容易
  暴露失败，但图片本身仍在微博声明的 20MB PNG 支持范围内。

适配器现已按真实状态机处理：等待图片库初始加载稳定，以数量恰好增加 1 绑定本次
上传，等待该唯一新条目完成，点击并验证唯一 `.is-selected`，再点击唯一“插入”；
全程只选择文件一次，任何歧义、超时或上传失败都停止且不自动重试。

## 第一次原草稿修复尝试与二次根因

上述状态机推送后，仅对原草稿 `4183864` 执行了一次覆盖尝试。一张 Word 图片
成功进入微博图片库，唯一新条目被选中且“插入”按钮完成点击，但适配器随后返回
`WEIBO_EDITOR_IMAGE_COUNT_UNCHANGED`，执行单记录为 `RESULT_UNKNOWN`。该次尝试没有
点击“保存草稿”，也没有点击公开发布；只读重开后，原草稿仍为 210 字、0 张真实
正文图、0 个 H2，且标题精确匹配数量仍为 1。

在阻断全部非 GET/HEAD/OPTIONS 请求的浏览器探测中，对图片库已有图片执行一次纯本地
插入，得到确定结构：

- 插入前：原始 `img` 节点 2 个，语义正文图片 0 张。
- 插入后：原始 `img` 节点 4 个，语义正文图片 1 张。
- 真实图片节点位于 `figure.wb-node-image`，内部唯一图片类为
  `image-view__body__image`。
- 其余 3 个 `img` 均为不可见、自然尺寸为 0 的 `ProseMirror-separator` 光标占位图。
- 刷新后恢复到原始 210 字、0 张语义正文图片；只拦截到统计请求，没有草稿保存或
  发布请求。

因此旧校验“所有 `img` 节点必须只增加 1”必然误报，而且旧 DOM token 读取还会把
文字段落内的 separator 当图片并吞掉文字。现已改为：只将顶层
`figure.wb-node-image` 内唯一的 `image-view__body__image` 计为正文图片；separator
完全忽略；出现任何未知非占位图片结构时 fail closed。

## 下一次真实动作前的门

1. 先提交并推送语义图片计数、separator 过滤和回归测试，核对远端 40 位 SHA。
2. 由于第一次修复尝试已产生图片库上传副作用，必须取得用户新的明确授权后，才可
   对草稿 `4183864` 再执行一次完整覆盖并点击一次“保存草稿”；不删除、不新建。
3. 保存后只读重开同一 ID，核验 7 张语义图片、5 个 H2、完整图文顺序和唯一
   草稿实体；未满足前不得开启生产微博投递。

## 自动化验证

- 语义图片计数与微博媒体定向测试：`40 passed`。
- 微博及交付链路回归：`132 passed`。
- 全量测试：`795 passed, 5 skipped`。
- Ruff：本轮相关文件通过。
- `git diff --check`：通过。

## 授权后的最终真实验收

用户明确授权后，系统基于同一不可变内容版本
`93bfa1120b1a7ea7b631e295673314f8fd1f8df41a80e7dead0c2204268105b1`，仅对原草稿
`4183864` 执行了一次完整覆盖：

- 投递计划：`f7d9e89b-f7f7-42af-bd72-5e1557f14275`，最终状态 `SUCCESS`。
- 执行单：`17feef38-5f43-4529-bf2a-22d61c5cfa8f`，最终状态 `DRAFT_SAVED`。
- 正文最终 DOM 校验通过：22 个文本段落、7 张语义正文图片、5 个 H2，图文顺序
  与冻结版本完全一致。
- 保存接口返回 HTTP 200、业务 code `100000`。
- 保存后适配器重开同一 draft ID 并再次通过完整 token 校验。
- 独立只读探测确认：精确标题草稿数量为 1、正文可读长度 2137、语义图片 7 张、
  H2 为 5 个、URL 仍为 `#/draft/4183864`。
- 文章映射审计状态 `SUCCEEDED`；没有新建第二篇草稿、没有设置封面、没有点击或
  请求公开发布。

结论：本指定 Word 在微博账号“天上的羊咩咩”上的“原草稿覆盖 → 7 图/5 H2 完整
写入 → 保存一次 → 重开同一实体验证”P0 链路真实验收通过。生产平台目录是否正式
启用微博投递，应在合并本分支并更新发布包时单独处理，不与本次真实平台副作用混做。

## 标准 Content Studio 流程复验与生产晋级

适配器级原草稿覆盖通过后，又使用正式 Content Studio API 完成了一次独立的
端到端复验，确认不是验收脚本特例：

- 源文件 SHA-256：`82fca555865565400edf0481ac9753a10fd241edb4f2dfe`（截断展示）。
- 冻结内容：30 个 canonical document block、7 张正文图片、5 个 H2；封面策略
  为 `NONE`。
- ContentDraft：`0da141ce-…`；DeliveryPlan：`a9acf3a0-…`。
- DeliveryOperation：`8fc72c71-…`，只执行一次，最终状态 `DRAFT_SAVED`。
- 微博返回唯一新云端草稿 ID `4184233`，保存接口 HTTP 200、业务 code `100000`。
- 计划最终状态 `SUCCESS`，文章映射审计状态 `SUCCEEDED`。
- 适配器在保存后重开同一 draft ID，再次通过 22 个文本段落、7 张语义正文图、
  5 个 H2 及完整顺序校验。
- `PUBLISH_AFTER_DRAFT` 与 `ACCOUNT_SESSIONS_ALLOW_PUBLIC_PUBLISH` 全程关闭；没有
  公开发布请求，也没有失败后的自动重试。

基于上述双重证据，微博从 v0.4.3 起进入生产草稿投递目录，格式能力只声明
`heading(H2)` 与 `image_order`。公开发布仍保持关闭，且 `publish_now()` 继续拒绝执行。
