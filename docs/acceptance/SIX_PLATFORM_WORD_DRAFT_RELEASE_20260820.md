# 六平台 Word 草稿发布版真实验收（2026-08-20）

## 验收范围

- 输入：`多设备共用显示器_川升CS40X_去AI优化发布版.docx`。
- 平台：小黑盒、ZOL、知乎、什么值得买、百家号、小红书。
- 模式：仅 `DRAFT`。
- 封面：`NONE`；封面不是本发布版的交付阻断项。
- 正文：22 个文本或二级标题块、7 张正文图片，合计 29 个平台图文块。
- 公开发布、删除草稿、清理 Cookie、清理 Profile：均未执行。
- `PUBLISH_AFTER_DRAFT`、`ACCOUNT_SESSIONS_ALLOW_PUBLIC_PUBLISH`、
  `LEGACY_UPLOAD_QUEUE_ENABLED`：验收进程中全部保持关闭。

## 冻结内容与批量计划

首次六目标计划：

- Draft：`fd991551-7846-42af-aaec-2828e0d8b85c`。
- Plan：`5ee2a5fa-f72f-440c-9ee2-dae54a3a7430`。
- Content hash：
  `c348840e809ef9aa10e484571e350a65306ccaa166d6b9b9f3752cee0f281144`。
- 六个目标均为 `DRAFT`，封面策略均为 `NONE`，公开确认令牌为零。

该批次真实完成了小黑盒、知乎和什么值得买。小红书真实新增了一条草稿，
草稿箱计数 `12 -> 13`，但旧代码错误地在无封面模式下读取不存在的封面标题，
把已存在草稿标为 `RESULT_UNKNOWN`。修复后使用同一 content hash、同一标题和
恢复现有草稿模式重开核验，草稿箱计数保持 `13 -> 13`，没有重复创建。

ZOL 在写入前因验收标题 49 字超过平台 35 字限制而安全失败；百家号在写入前
因会话过期安全失败。两者均没有进入对应平台的正文保存动作。随后将同一 Word
正文和 7 张受控图片冻结为平台共同支持的 27 字标题版本：

- Content hash：
  `4d866e3eed7f0d595eca228a61b3fe7c00796fbfc8605f16591e11210f8d7e0a`。
- 标题：`交付验收0820B｜多设备共用显示器：输入源与桌面空间`。
- ZOL Plan：`6ae1fec2-e227-4f2b-ad89-ff508b2c8f69`。
- 百家号 Plan：`d73eeb14-5cdb-451c-b365-0254bf1fbc8e`。

## 最终平台证据

| 平台 | 账号快照 | Operation | 最终状态 | 草稿证据 |
|---|---|---|---|---|
| 小黑盒 | 玩家52852173 | `1db54bbc-85b6-411b-96ee-0007c3f2c927` | `DRAFT_SAVED` | 22 个文字/标题块、7 张图片全部写入，唯一草稿卡与重开正文通过 |
| ZOL | 1wphk1 | `e10835ed-f2d7-4786-96b9-83c4b5827a16` | `DRAFT_SAVED` | 7/7 图片逐张内容校验，22 个文字/标题块与 29 个有序 token 重开通过 |
| 知乎 | Ulysses Yang | `d5309069-8d70-4be4-b988-4885d7092215` | `DRAFT_SAVED` | 草稿 ID、重开正文和 7 张图片通过 |
| 什么值得买 | 值友3424774480 | `d6e79898-ddf0-4bbc-ab00-2aca3de27cf3` | `DRAFT_SAVED` | 草稿 URL、正文有序结构和 7 张图片通过 |
| 百家号 | 他日若得脱身法生吃黄连苦也甜 | `858021ea-c2d0-469a-90ae-6fb72fb6843b` | `DRAFT_SAVED` | 草稿 URL 和文章映射 `SUCCEEDED`，正文与 7 张图片通过 |
| 小红书 | jayoma | `13d30730-82fd-44bc-a8d9-77b08b8ffe45` | `DRAFT_SAVED` | 同一草稿恢复核验，草稿计数不增加，7/7 图片和排版顺序通过 |

## 本轮修复

1. Word 导入默认封面改为 `NONE`。封面成为显式可选能力，不再阻断正文草稿。
2. 小红书无封面排版草稿使用草稿抽屉唯一精确标题作为证据，不再读取空的
   `.cover-outer-container` 标题节点。
3. ZOL 图片上传后每轮重新解析 TinyMCE iframe/body 和图片指纹；等待图片真实
   加载后再截图比对，截图 locator 失效时可通过同一浏览器会话在内存中读取图片，
   全程不记录 URL、响应或本机路径。
4. 基础流水线保留封面结果的明确状态；可选封面核验失败只产生警告，不自动
   重复保存已经存在的平台草稿。

## 结论与边界

六个平台均已用指定 Word 完成真实 `DRAFT_SAVED` 闭环。这里的“完成”指标题、
正文层级、图文顺序、7 张正文图片和平台草稿实体；不包含公开发布，也不把封面
算作本发布版的必选项。封面相关能力只有存在单独真实证据时才可标记为
`REAL_VERIFIED`，不能由本次无封面验收推断。

发布前自动验证：定向回归 `199 passed`；全仓 `747 passed, 2 skipped`；
变更文件 Ruff 关键错误检查和 `git diff --check` 通过。
