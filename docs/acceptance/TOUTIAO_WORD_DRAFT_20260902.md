# 头条号完整 Word 草稿验收（2026-09-02）

## 验收范围

- 模式：仅保存平台草稿，未执行公开发布。
- 入口：从草稿管理页点击平台原生“创作 → 文章”，未直接打开新建编辑 URL。
- 内容：指定 Word 冻结版本，共 29 个有序正文块，其中 17 个正文段落、5 个 H2、7 张正文图片。
- 封面：本轮不设置独立封面，正文首图未被移作封面。

## 真实结果

- 平台生成了唯一的新云端草稿 ID，并在草稿列表中可见。
- 按该 ID 重新打开编辑页后，标题与冻结版本一致。
- DOM 回读得到 29/29 个内容块与 7/7 张图片，块类型和图片位置顺序与冻结版本完全一致。
- 最终成功判定绑定该新草稿 ID；同名历史草稿的虚拟列表渲染不再造成假失败。

随后使用另一份不同排版 Word 做独立回归：生产解析器生成 30 个有序块
（16 个正文段落、8 个 H2、6 张图片）。平台自动保存响应全部成功；按唯一新
草稿 ID 重开后，30/30 个块与 6/6 张图片及顺序完全一致。草稿卡片延迟出现时，
最终判定改用“平台响应绑定的新 ID + 该 ID 精确重开完整图文”作为强证据，
不再产生假失败，也没有重复投递该文章。

## 安全边界

- 只执行一次平台原生退出以触发自动保存。
- 未点击公开发布。
- 对账阶段只重新打开既有草稿并读取结构，没有创建或覆盖其他草稿。
